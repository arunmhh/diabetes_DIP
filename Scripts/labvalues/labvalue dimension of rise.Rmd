---
title: "labvalue dimension of rise"
output: html_document
date: "2025-05-13"
---

```{r}
library(readxl)
library(tidyr)
library(dplyr)
library(ggstatsplot)
library(tidyverse)
library(readxl)
library(dlookr)
library(reshape2)
```



```{r}

gpt_full_filtered <- gpt_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
gpt_full_filtered
```

```{r}
gpt_full_filtered_grouped <- gpt_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 20.9 ~ "within normal reference",
      peak_labread >= 21 & peak_labread <= 62.9 ~ "1-2 times ULN",
      peak_labread >= 63 & peak_labread <= 127.9 ~ "3-5 times ULN",
      peak_labread >= 128 & peak_labread <= 212.9 ~ "6-9 times ULN",
      peak_labread >= 213 & peak_labread <= 1062.9 ~ "10-49 times ULN",
      peak_labread >= 1063 & peak_labread <= 2124.9 ~ "50-99 times ULN",
      peak_labread >= 2125 & peak_labread <= 10624.9 ~ "100-499 times ULN",
      peak_labread >= 10625 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
gpt_full_filtered_grouped
```




####labreads per Group count###
```{r}
labvalue_group_count <- gpt_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
labvalue_group_count
```



```{r}
labvalue_group_count$Labvalue_Group <-  factor(labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
all_patients_labvalue_dimension_of_peak_ALT <- labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "all patients_labvalue dimension of peak-ALT", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/alt_all_patients_labvalue_dimension_of_peak_ALT.svg", height = 8, width = 6.5)
print(all_patients_labvalue_dimension_of_peak_ALT)
dev.off()
```

#### Leber_Diclo ############

```{r}

ldiclo_gpt_full_filtered <- gpt_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Diclo")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
ldiclo_gpt_full_filtered
```


```{r}
ldiclo_gpt_full_filtered_grouped <- ldiclo_gpt_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 20.9 ~ "within normal reference",
      peak_labread >= 21 & peak_labread <= 62.9 ~ "1-2 times ULN",
      peak_labread >= 63 & peak_labread <= 127.9 ~ "3-5 times ULN",
      peak_labread >= 128 & peak_labread <= 212.9 ~ "6-9 times ULN",
      peak_labread >= 213 & peak_labread <= 1062.9 ~ "10-49 times ULN",
      peak_labread >= 1063 & peak_labread <= 2124.9 ~ "50-99 times ULN",
      peak_labread >= 2125 & peak_labread <= 10624.9 ~ "100-499 times ULN",
      peak_labread >= 10625 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
ldiclo_gpt_full_filtered_grouped
```


```{r}
ldiclo_labvalue_group_count <- ldiclo_gpt_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
ldiclo_labvalue_group_count
```


```{r}
ldiclo_labvalue_group_count$Labvalue_Group <-  factor(ldiclo_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
ldiclo_labvalue_dimension_of_peak_ALT <- ldiclo_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = ldiclo_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver diclo_labvalue dimension of peak-ALT", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/alt_ldiclo_labvalue_dimension_of_peak_ALT.svg", height = 8, width = 6.5)
print(ldiclo_labvalue_dimension_of_peak_ALT)
dev.off()
```



#### Leber_Ibu ############

```{r}

libu_gpt_full_filtered <- gpt_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Ibu")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
libu_gpt_full_filtered_grouped <- libu_gpt_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 20.9 ~ "within normal reference",
      peak_labread >= 21 & peak_labread <= 62.9 ~ "1-2 times ULN",
      peak_labread >= 63 & peak_labread <= 127.9 ~ "3-5 times ULN",
      peak_labread >= 128 & peak_labread <= 212.9 ~ "6-9 times ULN",
      peak_labread >= 213 & peak_labread <= 1062.9 ~ "10-49 times ULN",
      peak_labread >= 1063 & peak_labread <= 2124.9 ~ "50-99 times ULN",
      peak_labread >= 2125 & peak_labread <= 10624.9 ~ "100-499 times ULN",
      peak_labread >= 10625 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
libu_labvalue_group_count <- libu_gpt_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
libu_labvalue_group_count$Labvalue_Group <-  factor(libu_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
libu_labvalue_dimension_of_peak_ALT <-libu_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = libu_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver ibu_labvalue dimension of peak-ALT", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/alt_libu_labvalue_dimension_of_peak_ALT.svg", height = 8, width = 6.5)
print(libu_labvalue_dimension_of_peak_ALT)
dev.off()
```



#### Leber_Para ############

```{r}

lpara_gpt_full_filtered <- gpt_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Para")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
lpara_gpt_full_filtered_grouped <- lpara_gpt_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 20.9 ~ "within normal reference",
      peak_labread >= 21 & peak_labread <= 62.9 ~ "1-2 times ULN",
      peak_labread >= 63 & peak_labread <= 127.9 ~ "3-5 times ULN",
      peak_labread >= 128 & peak_labread <= 212.9 ~ "6-9 times ULN",
      peak_labread >= 213 & peak_labread <= 1062.9 ~ "10-49 times ULN",
      peak_labread >= 1063 & peak_labread <= 2124.9 ~ "50-99 times ULN",
      peak_labread >= 2125 & peak_labread <= 10624.9 ~ "100-499 times ULN",
      peak_labread >= 10625 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
lpara_labvalue_group_count <- lpara_gpt_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
lpara_labvalue_group_count$Labvalue_Group <-  factor(lpara_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
lpara_labvalue_dimension_of_peak_ALT <- lpara_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = lpara_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver_para labvalue dimension of peak-ALT", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  ) #-> liver_para_labvalue_dimension_of_peak_ALT


dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/alt_lpara_labvalue_dimension_of_peak_ALT.svg", height = 8, width = 6.5)
print(lpara_labvalue_dimension_of_peak_ALT)
dev.off()


```


### AST #######################################################################




```{r}

got_full_filtered <- got_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
got_full_filtered
```

```{r}
got_full_filtered_grouped <- got_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 20.9 ~ "within normal reference",
      peak_labread >= 21 & peak_labread <= 62.9 ~ "1-2 times ULN",
      peak_labread >= 63 & peak_labread <= 127.9 ~ "3-5 times ULN",
      peak_labread >= 128 & peak_labread <= 212.9 ~ "6-9 times ULN",
      peak_labread >= 213 & peak_labread <= 1062.9 ~ "10-49 times ULN",
      peak_labread >= 1063 & peak_labread <= 2124.9 ~ "50-99 times ULN",
      peak_labread >= 2125 & peak_labread <= 10624.9 ~ "100-499 times ULN",
      peak_labread >= 10625 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
got_full_filtered_grouped
```




####labreads per Group count###
```{r}
labvalue_group_count <- got_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
labvalue_group_count
```



```{r}
labvalue_group_count$Labvalue_Group <-  factor(labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
all_patients_labvalue_dimension_of_peak_ast <- labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "all patients_labvalue dimension of peak-ast", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/ast_all_patients_labvalue_dimension_of_peak_ast.svg", height = 8, width = 6.5)
print(all_patients_labvalue_dimension_of_peak_ast)
dev.off()
```

#### Leber_Diclo ############

```{r}

ldiclo_got_full_filtered <- got_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Diclo")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
ldiclo_got_full_filtered
```


```{r}
ldiclo_got_full_filtered_grouped <- ldiclo_got_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 20.9 ~ "within normal reference",
      peak_labread >= 21 & peak_labread <= 62.9 ~ "1-2 times ULN",
      peak_labread >= 63 & peak_labread <= 127.9 ~ "3-5 times ULN",
      peak_labread >= 128 & peak_labread <= 212.9 ~ "6-9 times ULN",
      peak_labread >= 213 & peak_labread <= 1062.9 ~ "10-49 times ULN",
      peak_labread >= 1063 & peak_labread <= 2124.9 ~ "50-99 times ULN",
      peak_labread >= 2125 & peak_labread <= 10624.9 ~ "100-499 times ULN",
      peak_labread >= 10625 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
ldiclo_got_full_filtered_grouped
```


```{r}
ldiclo_labvalue_group_count <- ldiclo_got_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
ldiclo_labvalue_group_count
```


```{r}
ldiclo_labvalue_group_count$Labvalue_Group <-  factor(ldiclo_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
ldiclo_labvalue_dimension_of_peak_ast <- ldiclo_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = ldiclo_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver diclo_labvalue dimension of peak-ast", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/ast_ldiclo_labvalue_dimension_of_peak_ast.svg", height = 8, width = 6.5)
print(ldiclo_labvalue_dimension_of_peak_ast)
dev.off()
```



#### Leber_Ibu ############

```{r}

libu_got_full_filtered <- got_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Ibu")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
libu_got_full_filtered_grouped <- libu_got_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 20.9 ~ "within normal reference",
      peak_labread >= 21 & peak_labread <= 62.9 ~ "1-2 times ULN",
      peak_labread >= 63 & peak_labread <= 127.9 ~ "3-5 times ULN",
      peak_labread >= 128 & peak_labread <= 212.9 ~ "6-9 times ULN",
      peak_labread >= 213 & peak_labread <= 1062.9 ~ "10-49 times ULN",
      peak_labread >= 1063 & peak_labread <= 2124.9 ~ "50-99 times ULN",
      peak_labread >= 2125 & peak_labread <= 10624.9 ~ "100-499 times ULN",
      peak_labread >= 10625 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
libu_labvalue_group_count <- libu_got_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
libu_labvalue_group_count$Labvalue_Group <-  factor(libu_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
libu_labvalue_dimension_of_peak_ast <-libu_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = libu_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver ibu_labvalue dimension of peak-ast", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/ast_libu_labvalue_dimension_of_peak_ast.svg", height = 8, width = 6.5)
print(libu_labvalue_dimension_of_peak_ast)
dev.off()
```



#### Leber_Para ############

```{r}

lpara_got_full_filtered <- got_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Para")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
lpara_got_full_filtered_grouped <- lpara_got_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 20.9 ~ "within normal reference",
      peak_labread >= 21 & peak_labread <= 62.9 ~ "1-2 times ULN",
      peak_labread >= 63 & peak_labread <= 127.9 ~ "3-5 times ULN",
      peak_labread >= 128 & peak_labread <= 212.9 ~ "6-9 times ULN",
      peak_labread >= 213 & peak_labread <= 1062.9 ~ "10-49 times ULN",
      peak_labread >= 1063 & peak_labread <= 2124.9 ~ "50-99 times ULN",
      peak_labread >= 2125 & peak_labread <= 10624.9 ~ "100-499 times ULN",
      peak_labread >= 10625 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
lpara_labvalue_group_count <- lpara_got_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
lpara_labvalue_group_count$Labvalue_Group <-  factor(lpara_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
lpara_labvalue_dimension_of_peak_ast <- lpara_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = lpara_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver_para labvalue dimension of peak-ast", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  ) #-> liver_para_labvalue_dimension_of_peak_ast


dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/ast_lpara_labvalue_dimension_of_peak_ast.svg", height = 8, width = 6.5)
print(lpara_labvalue_dimension_of_peak_ast)
dev.off()


```



### GGT #####################################################################



```{r}

ggt_full_filtered <- ggt_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
ggt_full_filtered
```

```{r}
ggt_full_filtered_grouped <- ggt_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 25.9 ~ "within normal reference",
      peak_labread >= 26 & peak_labread <= 77.9 ~ "1-2 times ULN",
      peak_labread >= 78 & peak_labread <= 155.9 ~ "3-5 times ULN",
      peak_labread >= 156 & peak_labread <= 259.9 ~ "6-9 times ULN",
      peak_labread >= 260 & peak_labread <= 1299.9 ~ "10-49 times ULN",
      peak_labread >= 1300 & peak_labread <= 2599.9 ~ "50-99 times ULN",
      peak_labread >= 2600 & peak_labread <= 12999.9 ~ "100-499 times ULN",
      peak_labread >= 13000 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
ggt_full_filtered_grouped
```




####labreads per Group count###
```{r}
labvalue_group_count <- ggt_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
labvalue_group_count
```



```{r}
labvalue_group_count$Labvalue_Group <-  factor(labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
all_patients_labvalue_dimension_of_peak_ggt <- labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "all patients_labvalue dimension of peak-ggt", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/ggt_all_patients_labvalue_dimension_of_peak_ggt.svg", height = 8, width = 6.5)
print(all_patients_labvalue_dimension_of_peak_ggt)
dev.off()
```

#### Leber_Diclo ############

```{r}

ldiclo_ggt_full_filtered <- ggt_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Diclo")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
ldiclo_ggt_full_filtered
```


```{r}
ldiclo_ggt_full_filtered_grouped <- ldiclo_ggt_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 25.9 ~ "within normal reference",
      peak_labread >= 26 & peak_labread <= 77.9 ~ "1-2 times ULN",
      peak_labread >= 78 & peak_labread <= 155.9 ~ "3-5 times ULN",
      peak_labread >= 156 & peak_labread <= 259.9 ~ "6-9 times ULN",
      peak_labread >= 260 & peak_labread <= 1299.9 ~ "10-49 times ULN",
      peak_labread >= 1300 & peak_labread <= 2599.9 ~ "50-99 times ULN",
      peak_labread >= 2600 & peak_labread <= 12999.9 ~ "100-499 times ULN",
      peak_labread >= 13000 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
ldiclo_ggt_full_filtered_grouped
```


```{r}
ldiclo_labvalue_group_count <- ldiclo_ggt_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
ldiclo_labvalue_group_count
```


```{r}
ldiclo_labvalue_group_count$Labvalue_Group <-  factor(ldiclo_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
ldiclo_labvalue_dimension_of_peak_ggt <- ldiclo_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = ldiclo_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver diclo_labvalue dimension of peak-ggt", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/ggt_ldiclo_labvalue_dimension_of_peak_ggt.svg", height = 8, width = 6.5)
print(ldiclo_labvalue_dimension_of_peak_ggt)
dev.off()
```



#### Leber_Ibu ############

```{r}

libu_ggt_full_filtered <- ggt_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Ibu")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
libu_ggt_full_filtered_grouped <- libu_ggt_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
     peak_labread <= 25.9 ~ "within normal reference",
      peak_labread >= 26 & peak_labread <= 77.9 ~ "1-2 times ULN",
      peak_labread >= 78 & peak_labread <= 155.9 ~ "3-5 times ULN",
      peak_labread >= 156 & peak_labread <= 259.9 ~ "6-9 times ULN",
      peak_labread >= 260 & peak_labread <= 1299.9 ~ "10-49 times ULN",
      peak_labread >= 1300 & peak_labread <= 2599.9 ~ "50-99 times ULN",
      peak_labread >= 2600 & peak_labread <= 12999.9 ~ "100-499 times ULN",
      peak_labread >= 13000 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
libu_labvalue_group_count <- libu_ggt_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
libu_labvalue_group_count$Labvalue_Group <-  factor(libu_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
libu_labvalue_dimension_of_peak_ggt <-libu_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = libu_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver ibu_labvalue dimension of peak-ggt", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/ggt_libu_labvalue_dimension_of_peak_ggt.svg", height = 8, width = 6.5)
print(libu_labvalue_dimension_of_peak_ggt)
dev.off()
```



#### Leber_Para ############

```{r}

lpara_ggt_full_filtered <- ggt_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Para")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
lpara_ggt_full_filtered_grouped <- lpara_ggt_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 25.9 ~ "within normal reference",
      peak_labread >= 26 & peak_labread <= 77.9 ~ "1-2 times ULN",
      peak_labread >= 78 & peak_labread <= 155.9 ~ "3-5 times ULN",
      peak_labread >= 156 & peak_labread <= 259.9 ~ "6-9 times ULN",
      peak_labread >= 260 & peak_labread <= 1299.9 ~ "10-49 times ULN",
      peak_labread >= 1300 & peak_labread <= 2599.9 ~ "50-99 times ULN",
      peak_labread >= 2600 & peak_labread <= 12999.9 ~ "100-499 times ULN",
      peak_labread >= 13000 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
lpara_labvalue_group_count <- lpara_ggt_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
lpara_labvalue_group_count$Labvalue_Group <-  factor(lpara_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
lpara_labvalue_dimension_of_peak_ggt <- lpara_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = lpara_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver_para labvalue dimension of peak-ggt", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  ) #-> liver_para_labvalue_dimension_of_peak_ggt


dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/ggt_lpara_labvalue_dimension_of_peak_ggt.svg", height = 8, width = 6.5)
print(lpara_labvalue_dimension_of_peak_ggt)
dev.off()


```



### ldh #####################################################################

```{r}

ldh_full_filtered <- ldh_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
ldh_full_filtered
```

```{r}
ldh_full_filtered_grouped <- ldh_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 382.9 ~ "within normal reference",
      peak_labread >= 383 & peak_labread <= 1148.9 ~ "1-2 times ULN",
      peak_labread >= 1149 & peak_labread <= 2297.9 ~ "3-5 times ULN",
      peak_labread >= 2298 & peak_labread <= 3829.9 ~ "6-9 times ULN",
      peak_labread >= 3830 & peak_labread <= 19149.9 ~ "10-49 times ULN",
      peak_labread >= 19150 & peak_labread <= 38299.9 ~ "50-99 times ULN",
      peak_labread >= 38300 & peak_labread <= 191499.9 ~ "100-499 times ULN",
      peak_labread >= 191500 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
ldh_full_filtered_grouped
```




####labreads per Group count###
```{r}
labvalue_group_count <- ldh_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
labvalue_group_count
```



```{r}
labvalue_group_count$Labvalue_Group <-  factor(labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
all_patients_labvalue_dimension_of_peak_ldh <- labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "all patients_labvalue dimension of peak-ldh", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/ldh_all_patients_labvalue_dimension_of_peak_ldh.svg", height = 8, width = 6.5)
print(all_patients_labvalue_dimension_of_peak_ldh)
dev.off()
```

#### Leber_Diclo ############

```{r}

ldiclo_ldh_full_filtered <- ldh_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Diclo")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
ldiclo_ldh_full_filtered
```


```{r}
ldiclo_ldh_full_filtered_grouped <- ldiclo_ldh_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 382.9 ~ "within normal reference",
      peak_labread >= 383 & peak_labread <= 1148.9 ~ "1-2 times ULN",
      peak_labread >= 1149 & peak_labread <= 2297.9 ~ "3-5 times ULN",
      peak_labread >= 2298 & peak_labread <= 3829.9 ~ "6-9 times ULN",
      peak_labread >= 3830 & peak_labread <= 19149.9 ~ "10-49 times ULN",
      peak_labread >= 19150 & peak_labread <= 38299.9 ~ "50-99 times ULN",
      peak_labread >= 38300 & peak_labread <= 191499.9 ~ "100-499 times ULN",
      peak_labread >= 191500 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
ldiclo_ldh_full_filtered_grouped
```


```{r}
ldiclo_labvalue_group_count <- ldiclo_ldh_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
ldiclo_labvalue_group_count
```


```{r}
ldiclo_labvalue_group_count$Labvalue_Group <-  factor(ldiclo_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
ldiclo_labvalue_dimension_of_peak_ldh <- ldiclo_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = ldiclo_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver diclo_labvalue dimension of peak-ldh", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/ldh_ldiclo_labvalue_dimension_of_peak_ldh.svg", height = 8, width = 6.5)
print(ldiclo_labvalue_dimension_of_peak_ldh)
dev.off()
```



#### Leber_Ibu ############

```{r}

libu_ldh_full_filtered <- ldh_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Ibu")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
libu_ldh_full_filtered_grouped <- libu_ldh_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 382.9 ~ "within normal reference",
      peak_labread >= 383 & peak_labread <= 1148.9 ~ "1-2 times ULN",
      peak_labread >= 1149 & peak_labread <= 2297.9 ~ "3-5 times ULN",
      peak_labread >= 2298 & peak_labread <= 3829.9 ~ "6-9 times ULN",
      peak_labread >= 3830 & peak_labread <= 19149.9 ~ "10-49 times ULN",
      peak_labread >= 19150 & peak_labread <= 38299.9 ~ "50-99 times ULN",
      peak_labread >= 38300 & peak_labread <= 191499.9 ~ "100-499 times ULN",
      peak_labread >= 191500 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
libu_labvalue_group_count <- libu_ldh_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
libu_labvalue_group_count$Labvalue_Group <-  factor(libu_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
libu_labvalue_dimension_of_peak_ldh <-libu_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = libu_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver ibu_labvalue dimension of peak-ldh", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/ldh_libu_labvalue_dimension_of_peak_ldh.svg", height = 8, width = 6.5)
print(libu_labvalue_dimension_of_peak_ldh)
dev.off()
```



#### Leber_Para ############

```{r}

lpara_ldh_full_filtered <- ldh_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Para")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
lpara_ldh_full_filtered_grouped <- lpara_ldh_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 382.9 ~ "within normal reference",
      peak_labread >= 383 & peak_labread <= 1148.9 ~ "1-2 times ULN",
      peak_labread >= 1149 & peak_labread <= 2297.9 ~ "3-5 times ULN",
      peak_labread >= 2298 & peak_labread <= 3829.9 ~ "6-9 times ULN",
      peak_labread >= 3830 & peak_labread <= 19149.9 ~ "10-49 times ULN",
      peak_labread >= 19150 & peak_labread <= 38299.9 ~ "50-99 times ULN",
      peak_labread >= 38300 & peak_labread <= 191499.9 ~ "100-499 times ULN",
      peak_labread >= 191500 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
lpara_labvalue_group_count <- lpara_ldh_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
lpara_labvalue_group_count$Labvalue_Group <-  factor(lpara_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
lpara_labvalue_dimension_of_peak_ldh <- lpara_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = lpara_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver_para labvalue dimension of peak-ldh", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  ) #-> liver_para_labvalue_dimension_of_peak_ldh


dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/ldh_lpara_labvalue_dimension_of_peak_ldh.svg", height = 8, width = 6.5)
print(lpara_labvalue_dimension_of_peak_ldh)
dev.off()


```



### ck #####################################################################

```{r}

ck_full_filtered <- ck_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
ck_full_filtered
```

```{r}
ck_full_filtered_grouped <- ck_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 77.9 ~ "within normal reference",
      peak_labread >= 78 & peak_labread <= 233.9 ~ "1-2 times ULN",
      peak_labread >= 234 & peak_labread <= 467.9 ~ "3-5 times ULN",
      peak_labread >= 468 & peak_labread <= 770.9 ~ "6-9 times ULN",
      peak_labread >= 780 & peak_labread <= 3899.9 ~ "10-49 times ULN",
      peak_labread >= 3900 & peak_labread <= 7799.9 ~ "50-99 times ULN",
      peak_labread >= 7800 & peak_labread <= 38999.9 ~ "100-499 times ULN",
      peak_labread >= 39000 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
ck_full_filtered_grouped
```




####labreads per Group count###
```{r}
labvalue_group_count <- ck_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
labvalue_group_count
```



```{r}
labvalue_group_count$Labvalue_Group <-  factor(labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
all_patients_labvalue_dimension_of_peak_ck <- labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "all patients_labvalue dimension of peak-ck", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/ck_all_patients_labvalue_dimension_of_peak_ck.svg", height = 8, width = 6.5)
print(all_patients_labvalue_dimension_of_peak_ck)
dev.off()
```

#### Leber_Diclo ############

```{r}

ldiclo_ck_full_filtered <- ck_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Diclo")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
ldiclo_ck_full_filtered
```


```{r}
ldiclo_ck_full_filtered_grouped <- ldiclo_ck_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 77.9 ~ "within normal reference",
      peak_labread >= 78 & peak_labread <= 233.9 ~ "1-2 times ULN",
      peak_labread >= 234 & peak_labread <= 467.9 ~ "3-5 times ULN",
      peak_labread >= 468 & peak_labread <= 770.9 ~ "6-9 times ULN",
      peak_labread >= 780 & peak_labread <= 3899.9 ~ "10-49 times ULN",
      peak_labread >= 3900 & peak_labread <= 7799.9 ~ "50-99 times ULN",
      peak_labread >= 7800 & peak_labread <= 38999.9 ~ "100-499 times ULN",
      peak_labread >= 39000 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
ldiclo_ck_full_filtered_grouped
```


```{r}
ldiclo_labvalue_group_count <- ldiclo_ck_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
ldiclo_labvalue_group_count
```


```{r}
ldiclo_labvalue_group_count$Labvalue_Group <-  factor(ldiclo_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
ldiclo_labvalue_dimension_of_peak_ck <- ldiclo_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = ldiclo_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver diclo_labvalue dimension of peak-ck", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/ck_ldiclo_labvalue_dimension_of_peak_ck.svg", height = 8, width = 6.5)
print(ldiclo_labvalue_dimension_of_peak_ck)
dev.off()
```



#### Leber_Ibu ############

```{r}

libu_ck_full_filtered <- ck_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Ibu")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
libu_ck_full_filtered_grouped <- libu_ck_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 77.9 ~ "within normal reference",
      peak_labread >= 78 & peak_labread <= 233.9 ~ "1-2 times ULN",
      peak_labread >= 234 & peak_labread <= 467.9 ~ "3-5 times ULN",
      peak_labread >= 468 & peak_labread <= 770.9 ~ "6-9 times ULN",
      peak_labread >= 780 & peak_labread <= 3899.9 ~ "10-49 times ULN",
      peak_labread >= 3900 & peak_labread <= 7799.9 ~ "50-99 times ULN",
      peak_labread >= 7800 & peak_labread <= 38999.9 ~ "100-499 times ULN",
      peak_labread >= 39000 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
libu_labvalue_group_count <- libu_ck_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
libu_labvalue_group_count$Labvalue_Group <-  factor(libu_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
libu_labvalue_dimension_of_peak_ck <-libu_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = libu_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver ibu_labvalue dimension of peak-ck", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/ck_libu_labvalue_dimension_of_peak_ck.svg", height = 8, width = 6.5)
print(libu_labvalue_dimension_of_peak_ck)
dev.off()
```



#### Leber_Para ############

```{r}

lpara_ck_full_filtered <- ck_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Para")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
lpara_ck_full_filtered_grouped <- lpara_ck_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 77.9 ~ "within normal reference",
      peak_labread >= 78 & peak_labread <= 233.9 ~ "1-2 times ULN",
      peak_labread >= 234 & peak_labread <= 467.9 ~ "3-5 times ULN",
      peak_labread >= 468 & peak_labread <= 770.9 ~ "6-9 times ULN",
      peak_labread >= 780 & peak_labread <= 3899.9 ~ "10-49 times ULN",
      peak_labread >= 3900 & peak_labread <= 7799.9 ~ "50-99 times ULN",
      peak_labread >= 7800 & peak_labread <= 38999.9 ~ "100-499 times ULN",
      peak_labread >= 39000 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
lpara_labvalue_group_count <- lpara_ck_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
lpara_labvalue_group_count$Labvalue_Group <-  factor(lpara_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
lpara_labvalue_dimension_of_peak_ck <- lpara_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = lpara_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver_para labvalue dimension of peak-ck", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  ) #-> liver_para_labvalue_dimension_of_peak_ck


dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/ck_lpara_labvalue_dimension_of_peak_ck.svg", height = 8, width = 6.5)
print(lpara_labvalue_dimension_of_peak_ck)
dev.off()


```


### ap #####################################################################

```{r}

ap_full_filtered <- ap_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
ap_full_filtered
```

```{r}
ap_full_filtered_grouped <- ap_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 20.9 ~ "within normal reference",
      peak_labread >= 119 & peak_labread <= 356.9 ~ "1-2 times ULN",
      peak_labread >= 357 & peak_labread <= 713.9 ~ "3-5 times ULN",
      peak_labread >= 714 & peak_labread <= 1189.9 ~ "6-9 times ULN",
      peak_labread >= 1190 & peak_labread <= 5949.9 ~ "10-49 times ULN",
      peak_labread >= 5950 & peak_labread <= 11899.9 ~ "50-99 times ULN",
      peak_labread >= 11900 & peak_labread <= 59499.9 ~ "100-499 times ULN",
      peak_labread >= 59500 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
ap_full_filtered_grouped
```




####labreads per Group count###
```{r}
labvalue_group_count <- ap_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
labvalue_group_count
```



```{r}
labvalue_group_count$Labvalue_Group <-  factor(labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
all_patients_labvalue_dimension_of_peak_ap <- labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "all patients_labvalue dimension of peak-ap", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/ap_all_patients_labvalue_dimension_of_peak_ap.svg", height = 8, width = 6.5)
print(all_patients_labvalue_dimension_of_peak_ap)
dev.off()
```

#### Leber_Diclo ############

```{r}

ldiclo_ap_full_filtered <- ap_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Diclo")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
ldiclo_ap_full_filtered
```


```{r}
ldiclo_ap_full_filtered_grouped <- ldiclo_ap_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
     peak_labread <= 20.9 ~ "within normal reference",
      peak_labread >= 119 & peak_labread <= 356.9 ~ "1-2 times ULN",
      peak_labread >= 357 & peak_labread <= 713.9 ~ "3-5 times ULN",
      peak_labread >= 714 & peak_labread <= 1189.9 ~ "6-9 times ULN",
      peak_labread >= 1190 & peak_labread <= 5949.9 ~ "10-49 times ULN",
      peak_labread >= 5950 & peak_labread <= 11899.9 ~ "50-99 times ULN",
      peak_labread >= 11900 & peak_labread <= 59499.9 ~ "100-499 times ULN",
      peak_labread >= 59500 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
ldiclo_ap_full_filtered_grouped
```


```{r}
ldiclo_labvalue_group_count <- ldiclo_ap_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
ldiclo_labvalue_group_count
```


```{r}
ldiclo_labvalue_group_count$Labvalue_Group <-  factor(ldiclo_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
ldiclo_labvalue_dimension_of_peak_ap <- ldiclo_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = ldiclo_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver diclo_labvalue dimension of peak-ap", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/ap_ldiclo_labvalue_dimension_of_peak_ap.svg", height = 8, width = 6.5)
print(ldiclo_labvalue_dimension_of_peak_ap)
dev.off()
```



#### Leber_Ibu ############

```{r}

libu_ap_full_filtered <- ap_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Ibu")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
libu_ap_full_filtered_grouped <- libu_ap_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 20.9 ~ "within normal reference",
      peak_labread >= 119 & peak_labread <= 356.9 ~ "1-2 times ULN",
      peak_labread >= 357 & peak_labread <= 713.9 ~ "3-5 times ULN",
      peak_labread >= 714 & peak_labread <= 1189.9 ~ "6-9 times ULN",
      peak_labread >= 1190 & peak_labread <= 5949.9 ~ "10-49 times ULN",
      peak_labread >= 5950 & peak_labread <= 11899.9 ~ "50-99 times ULN",
      peak_labread >= 11900 & peak_labread <= 59499.9 ~ "100-499 times ULN",
      peak_labread >= 59500 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
libu_labvalue_group_count <- libu_ap_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
libu_labvalue_group_count$Labvalue_Group <-  factor(libu_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
libu_labvalue_dimension_of_peak_ap <-libu_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = libu_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver ibu_labvalue dimension of peak-ap", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/ap_libu_labvalue_dimension_of_peak_ap.svg", height = 8, width = 6.5)
print(libu_labvalue_dimension_of_peak_ap)
dev.off()
```



#### Leber_Para ############

```{r}

lpara_ap_full_filtered <- ap_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Para")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
lpara_ap_full_filtered_grouped <- lpara_ap_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 20.9 ~ "within normal reference",
      peak_labread >= 119 & peak_labread <= 356.9 ~ "1-2 times ULN",
      peak_labread >= 357 & peak_labread <= 713.9 ~ "3-5 times ULN",
      peak_labread >= 714 & peak_labread <= 1189.9 ~ "6-9 times ULN",
      peak_labread >= 1190 & peak_labread <= 5949.9 ~ "10-49 times ULN",
      peak_labread >= 5950 & peak_labread <= 11899.9 ~ "50-99 times ULN",
      peak_labread >= 11900 & peak_labread <= 59499.9 ~ "100-499 times ULN",
      peak_labread >= 59500 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
lpara_labvalue_group_count <- lpara_ap_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
lpara_labvalue_group_count$Labvalue_Group <-  factor(lpara_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
lpara_labvalue_dimension_of_peak_ap <- lpara_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = lpara_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver_para labvalue dimension of peak-ap", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  ) #-> liver_para_labvalue_dimension_of_peak_ap


dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/ap_lpara_labvalue_dimension_of_peak_ap.svg", height = 8, width = 6.5)
print(lpara_labvalue_dimension_of_peak_ap)
dev.off()


```


### quick #####################################################################

```{r}

quick_full_filtered <- quick_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
quick_full_filtered
```

```{r}
quick_full_filtered_grouped <- quick_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 84.9 ~ "within normal reference",
      peak_labread >= 85 & peak_labread <= 254.9 ~ "1-2 times ULN",
      peak_labread >= 255 & peak_labread <= 509.9 ~ "3-5 times ULN",
      peak_labread >= 510 & peak_labread <= 849.9 ~ "6-9 times ULN",
      peak_labread >= 850 & peak_labread <= 4249.9 ~ "10-49 times ULN",
      peak_labread >= 4250 & peak_labread <= 8499.9 ~ "50-99 times ULN",
      peak_labread >= 8500 & peak_labread <= 42499.9 ~ "100-499 times ULN",
      peak_labread >= 42500 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
quick_full_filtered_grouped
```




####labreads per Group count###
```{r}
labvalue_group_count <- quick_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
labvalue_group_count
```



```{r}
labvalue_group_count$Labvalue_Group <-  factor(labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
all_patients_labvalue_dimension_of_peak_quick <- labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "all patients_labvalue dimension of peak-quick", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/quick_all_patients_labvalue_dimension_of_peak_quick.svg", height = 8, width = 6.5)
print(all_patients_labvalue_dimension_of_peak_quick)
dev.off()
```

#### Leber_Diclo ############

```{r}

ldiclo_quick_full_filtered <- quick_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Diclo")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
ldiclo_quick_full_filtered
```


```{r}
ldiclo_quick_full_filtered_grouped <- ldiclo_quick_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 84.9 ~ "within normal reference",
      peak_labread >= 85 & peak_labread <= 254.9 ~ "1-2 times ULN",
      peak_labread >= 255 & peak_labread <= 509.9 ~ "3-5 times ULN",
      peak_labread >= 510 & peak_labread <= 849.9 ~ "6-9 times ULN",
      peak_labread >= 850 & peak_labread <= 4249.9 ~ "10-49 times ULN",
      peak_labread >= 4250 & peak_labread <= 8499.9 ~ "50-99 times ULN",
      peak_labread >= 8500 & peak_labread <= 42499.9 ~ "100-499 times ULN",
      peak_labread >= 42500 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
ldiclo_quick_full_filtered_grouped
```


```{r}
ldiclo_labvalue_group_count <- ldiclo_quick_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
ldiclo_labvalue_group_count
```


```{r}
ldiclo_labvalue_group_count$Labvalue_Group <-  factor(ldiclo_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
ldiclo_labvalue_dimension_of_peak_quick <- ldiclo_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = ldiclo_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver diclo_labvalue dimension of peak-quick", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/quick_ldiclo_labvalue_dimension_of_peak_quick.svg", height = 8, width = 6.5)
print(ldiclo_labvalue_dimension_of_peak_quick)
dev.off()
```



#### Leber_Ibu ############

```{r}

libu_quick_full_filtered <- quick_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Ibu")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
libu_quick_full_filtered_grouped <- libu_quick_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 84.9 ~ "within normal reference",
      peak_labread >= 85 & peak_labread <= 254.9 ~ "1-2 times ULN",
      peak_labread >= 255 & peak_labread <= 509.9 ~ "3-5 times ULN",
      peak_labread >= 510 & peak_labread <= 849.9 ~ "6-9 times ULN",
      peak_labread >= 850 & peak_labread <= 4249.9 ~ "10-49 times ULN",
      peak_labread >= 4250 & peak_labread <= 8499.9 ~ "50-99 times ULN",
      peak_labread >= 8500 & peak_labread <= 42499.9 ~ "100-499 times ULN",
      peak_labread >= 42500 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
libu_labvalue_group_count <- libu_quick_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
libu_labvalue_group_count$Labvalue_Group <-  factor(libu_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
libu_labvalue_dimension_of_peak_quick <-libu_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = libu_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver ibu_labvalue dimension of peak-quick", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/quick_libu_labvalue_dimension_of_peak_quick.svg", height = 8, width = 6.5)
print(libu_labvalue_dimension_of_peak_quick)
dev.off()
```



#### Leber_Para ############

```{r}

lpara_quick_full_filtered <- quick_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Para")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
lpara_quick_full_filtered_grouped <- lpara_quick_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 84.9 ~ "within normal reference",
      peak_labread >= 85 & peak_labread <= 254.9 ~ "1-2 times ULN",
      peak_labread >= 255 & peak_labread <= 509.9 ~ "3-5 times ULN",
      peak_labread >= 510 & peak_labread <= 849.9 ~ "6-9 times ULN",
      peak_labread >= 850 & peak_labread <= 4249.9 ~ "10-49 times ULN",
      peak_labread >= 4250 & peak_labread <= 8499.9 ~ "50-99 times ULN",
      peak_labread >= 8500 & peak_labread <= 42499.9 ~ "100-499 times ULN",
      peak_labread >= 42500 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
lpara_labvalue_group_count <- lpara_quick_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
lpara_labvalue_group_count$Labvalue_Group <-  factor(lpara_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
lpara_labvalue_dimension_of_peak_quick <- lpara_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = lpara_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver_para labvalue dimension of peak-quick", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  ) #-> liver_para_labvalue_dimension_of_peak_quick


dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/quick_lpara_labvalue_dimension_of_peak_quick.svg", height = 8, width = 6.5)
print(lpara_labvalue_dimension_of_peak_quick)
dev.off()


```


### ptt #####################################################################

```{r}

ptt_full_filtered <- ptt_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
ptt_full_filtered
```

```{r}
ptt_full_filtered_grouped <- ptt_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 30.9 ~ "within normal reference",
      peak_labread >= 31 & peak_labread <= 92.9 ~ "1-2 times ULN",
      peak_labread >= 93 & peak_labread <= 185.9 ~ "3-5 times ULN",
      peak_labread >= 186 & peak_labread <= 309.9 ~ "6-9 times ULN",
      peak_labread >= 310 & peak_labread <= 1549.9 ~ "10-49 times ULN",
      peak_labread >= 1550 & peak_labread <= 2123.9 ~ "50-99 times ULN",
      peak_labread >= 3100 & peak_labread <= 3099.9 ~ "100-499 times ULN",
      peak_labread >= 15500 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
ptt_full_filtered_grouped
```




####labreads per Group count###
```{r}
labvalue_group_count <- ptt_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
labvalue_group_count
```



```{r}
labvalue_group_count$Labvalue_Group <-  factor(labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
all_patients_labvalue_dimension_of_peak_ptt <- labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "all patients_labvalue dimension of peak-ptt", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/ptt_all_patients_labvalue_dimension_of_peak_ptt.svg", height = 8, width = 6.5)
print(all_patients_labvalue_dimension_of_peak_ptt)
dev.off()
```

#### Leber_Diclo ############

```{r}

ldiclo_ptt_full_filtered <- ptt_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Diclo")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
ldiclo_ptt_full_filtered
```


```{r}
ldiclo_ptt_full_filtered_grouped <- ldiclo_ptt_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 30.9 ~ "within normal reference",
      peak_labread >= 31 & peak_labread <= 92.9 ~ "1-2 times ULN",
      peak_labread >= 93 & peak_labread <= 185.9 ~ "3-5 times ULN",
      peak_labread >= 186 & peak_labread <= 309.9 ~ "6-9 times ULN",
      peak_labread >= 310 & peak_labread <= 1549.9 ~ "10-49 times ULN",
      peak_labread >= 1550 & peak_labread <= 2123.9 ~ "50-99 times ULN",
      peak_labread >= 3100 & peak_labread <= 3099.9 ~ "100-499 times ULN",
      peak_labread >= 15500 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
ldiclo_ptt_full_filtered_grouped
```


```{r}
ldiclo_labvalue_group_count <- ldiclo_ptt_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
ldiclo_labvalue_group_count
```


```{r}
ldiclo_labvalue_group_count$Labvalue_Group <-  factor(ldiclo_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
ldiclo_labvalue_dimension_of_peak_ptt <- ldiclo_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = ldiclo_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver diclo_labvalue dimension of peak-ptt", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/ptt_ldiclo_labvalue_dimension_of_peak_ptt.svg", height = 8, width = 6.5)
print(ldiclo_labvalue_dimension_of_peak_ptt)
dev.off()
```



#### Leber_Ibu ############

```{r}

libu_ptt_full_filtered <- ptt_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Ibu")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
libu_ptt_full_filtered_grouped <- libu_ptt_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 30.9 ~ "within normal reference",
      peak_labread >= 31 & peak_labread <= 92.9 ~ "1-2 times ULN",
      peak_labread >= 93 & peak_labread <= 185.9 ~ "3-5 times ULN",
      peak_labread >= 186 & peak_labread <= 309.9 ~ "6-9 times ULN",
      peak_labread >= 310 & peak_labread <= 1549.9 ~ "10-49 times ULN",
      peak_labread >= 1550 & peak_labread <= 2123.9 ~ "50-99 times ULN",
      peak_labread >= 3100 & peak_labread <= 3099.9 ~ "100-499 times ULN",
      peak_labread >= 15500 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
libu_labvalue_group_count <- libu_ptt_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
libu_labvalue_group_count$Labvalue_Group <-  factor(libu_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
libu_labvalue_dimension_of_peak_ptt <-libu_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = libu_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver ibu_labvalue dimension of peak-ptt", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/ptt_libu_labvalue_dimension_of_peak_ptt.svg", height = 8, width = 6.5)
print(libu_labvalue_dimension_of_peak_ptt)
dev.off()
```



#### Leber_Para ############

```{r}

lpara_ptt_full_filtered <- ptt_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Para")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
lpara_ptt_full_filtered_grouped <- lpara_ptt_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 30.9 ~ "within normal reference",
      peak_labread >= 31 & peak_labread <= 92.9 ~ "1-2 times ULN",
      peak_labread >= 93 & peak_labread <= 185.9 ~ "3-5 times ULN",
      peak_labread >= 186 & peak_labread <= 309.9 ~ "6-9 times ULN",
      peak_labread >= 310 & peak_labread <= 1549.9 ~ "10-49 times ULN",
      peak_labread >= 1550 & peak_labread <= 2123.9 ~ "50-99 times ULN",
      peak_labread >= 3100 & peak_labread <= 3099.9 ~ "100-499 times ULN",
      peak_labread >= 15500 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
lpara_labvalue_group_count <- lpara_ptt_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
lpara_labvalue_group_count$Labvalue_Group <-  factor(lpara_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
lpara_labvalue_dimension_of_peak_ptt <- lpara_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = lpara_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver_para labvalue dimension of peak-ptt", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  ) #-> liver_para_labvalue_dimension_of_peak_ptt


dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/ptt_lpara_labvalue_dimension_of_peak_ptt.svg", height = 8, width = 6.5)
print(lpara_labvalue_dimension_of_peak_ptt)
dev.off()


```


### leukozyten #####################################################################

```{r}

leukozyten_full_filtered <- leukozyten_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
leukozyten_full_filtered
```

```{r}
leukozyten_full_filtered_grouped <- leukozyten_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 7999.9 ~ "within normal reference",
      peak_labread >= 8000 & peak_labread <= 23999.9 ~ "1-2 times ULN",
      peak_labread >= 24000 & peak_labread <= 47999.9 ~ "3-5 times ULN",
      peak_labread >= 48000 & peak_labread <= 79999.9 ~ "6-9 times ULN",
      peak_labread >= 80000 & peak_labread <= 399999.9 ~ "10-49 times ULN",
      peak_labread >= 400000 & peak_labread <= 799999.9 ~ "50-99 times ULN",
      peak_labread >= 800000 & peak_labread <= 3999999.9 ~ "100-499 times ULN",
      peak_labread >= 4000000 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
leukozyten_full_filtered_grouped
```




####labreads per Group count###
```{r}
labvalue_group_count <- leukozyten_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
labvalue_group_count
```



```{r}
labvalue_group_count$Labvalue_Group <-  factor(labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
all_patients_labvalue_dimension_of_peak_leukozyten <- labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "all patients_labvalue dimension of peak-leukozyten", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/leukozyten_all_patients_labvalue_dimension_of_peak_leukozyten.svg", height = 8, width = 6.5)
print(all_patients_labvalue_dimension_of_peak_leukozyten)
dev.off()
```

#### Leber_Diclo ############

```{r}

ldiclo_leukozyten_full_filtered <- leukozyten_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Diclo")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
ldiclo_leukozyten_full_filtered
```


```{r}
ldiclo_leukozyten_full_filtered_grouped <- ldiclo_leukozyten_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 7999.9 ~ "within normal reference",
      peak_labread >= 8000 & peak_labread <= 23999.9 ~ "1-2 times ULN",
      peak_labread >= 24000 & peak_labread <= 47999.9 ~ "3-5 times ULN",
      peak_labread >= 48000 & peak_labread <= 79999.9 ~ "6-9 times ULN",
      peak_labread >= 80000 & peak_labread <= 399999.9 ~ "10-49 times ULN",
      peak_labread >= 400000 & peak_labread <= 799999.9 ~ "50-99 times ULN",
      peak_labread >= 800000 & peak_labread <= 3999999.9 ~ "100-499 times ULN",
      peak_labread >= 4000000 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
ldiclo_leukozyten_full_filtered_grouped
```


```{r}
ldiclo_labvalue_group_count <- ldiclo_leukozyten_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
ldiclo_labvalue_group_count
```


```{r}
ldiclo_labvalue_group_count$Labvalue_Group <-  factor(ldiclo_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
ldiclo_labvalue_dimension_of_peak_leukozyten <- ldiclo_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = ldiclo_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver diclo_labvalue dimension of peak-leukozyten", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/leukozyten_ldiclo_labvalue_dimension_of_peak_leukozyten.svg", height = 8, width = 6.5)
print(ldiclo_labvalue_dimension_of_peak_leukozyten)
dev.off()
```



#### Leber_Ibu ############

```{r}

libu_leukozyten_full_filtered <- leukozyten_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Ibu")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
libu_leukozyten_full_filtered_grouped <- libu_leukozyten_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 7999.9 ~ "within normal reference",
      peak_labread >= 8000 & peak_labread <= 23999.9 ~ "1-2 times ULN",
      peak_labread >= 24000 & peak_labread <= 47999.9 ~ "3-5 times ULN",
      peak_labread >= 48000 & peak_labread <= 79999.9 ~ "6-9 times ULN",
      peak_labread >= 80000 & peak_labread <= 399999.9 ~ "10-49 times ULN",
      peak_labread >= 400000 & peak_labread <= 799999.9 ~ "50-99 times ULN",
      peak_labread >= 800000 & peak_labread <= 3999999.9 ~ "100-499 times ULN",
      peak_labread >= 4000000 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
libu_labvalue_group_count <- libu_leukozyten_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
libu_labvalue_group_count$Labvalue_Group <-  factor(libu_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
libu_labvalue_dimension_of_peak_leukozyten <-libu_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = libu_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver ibu_labvalue dimension of peak-leukozyten", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/leukozyten_libu_labvalue_dimension_of_peak_leukozyten.svg", height = 8, width = 6.5)
print(libu_labvalue_dimension_of_peak_leukozyten)
dev.off()
```



#### Leber_Para ############

```{r}

lpara_leukozyten_full_filtered <- leukozyten_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Para")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
lpara_leukozyten_full_filtered_grouped <- lpara_leukozyten_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 7999.9 ~ "within normal reference",
      peak_labread >= 8000 & peak_labread <= 23999.9 ~ "1-2 times ULN",
      peak_labread >= 24000 & peak_labread <= 47999.9 ~ "3-5 times ULN",
      peak_labread >= 48000 & peak_labread <= 79999.9 ~ "6-9 times ULN",
      peak_labread >= 80000 & peak_labread <= 399999.9 ~ "10-49 times ULN",
      peak_labread >= 400000 & peak_labread <= 799999.9 ~ "50-99 times ULN",
      peak_labread >= 800000 & peak_labread <= 3999999.9 ~ "100-499 times ULN",
      peak_labread >= 4000000 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
lpara_labvalue_group_count <- lpara_leukozyten_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
lpara_labvalue_group_count$Labvalue_Group <-  factor(lpara_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
lpara_labvalue_dimension_of_peak_leukozyten <- lpara_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = lpara_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver_para labvalue dimension of peak-leukozyten", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  ) #-> liver_para_labvalue_dimension_of_peak_leukozyten


dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/leukozyten_lpara_labvalue_dimension_of_peak_leukozyten.svg", height = 8, width = 6.5)
print(lpara_labvalue_dimension_of_peak_leukozyten)
dev.off()


```



### hb #####################################################################

```{r}

hb_full_filtered <- hb_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
hb_full_filtered
```

```{r}
hb_full_filtered_grouped <- hb_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 14.9 ~ "within normal reference",
      peak_labread >= 15 & peak_labread <= 44.9 ~ "1-2 times ULN",
      peak_labread >= 45 & peak_labread <= 89.9 ~ "3-5 times ULN",
      peak_labread >= 90 & peak_labread <= 149.9 ~ "6-9 times ULN",
      peak_labread >= 150 & peak_labread <= 749.9 ~ "10-49 times ULN",
      peak_labread >= 750 & peak_labread <= 1499.9 ~ "50-99 times ULN",
      peak_labread >= 1500 & peak_labread <= 7499.9 ~ "100-499 times ULN",
      peak_labread >= 7500 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
hb_full_filtered_grouped
```




####labreads per Group count###
```{r}
labvalue_group_count <- hb_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
labvalue_group_count
```



```{r}
labvalue_group_count$Labvalue_Group <-  factor(labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
all_patients_labvalue_dimension_of_peak_hb <- labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "all patients_labvalue dimension of peak-hb", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/hb_all_patients_labvalue_dimension_of_peak_hb.svg", height = 8, width = 6.5)
print(all_patients_labvalue_dimension_of_peak_hb)
dev.off()
```

#### Leber_Diclo ############

```{r}

ldiclo_hb_full_filtered <- hb_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Diclo")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
ldiclo_hb_full_filtered
```


```{r}
ldiclo_hb_full_filtered_grouped <- ldiclo_hb_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 14.9 ~ "within normal reference",
      peak_labread >= 15 & peak_labread <= 44.9 ~ "1-2 times ULN",
      peak_labread >= 45 & peak_labread <= 89.9 ~ "3-5 times ULN",
      peak_labread >= 90 & peak_labread <= 149.9 ~ "6-9 times ULN",
      peak_labread >= 150 & peak_labread <= 749.9 ~ "10-49 times ULN",
      peak_labread >= 750 & peak_labread <= 1499.9 ~ "50-99 times ULN",
      peak_labread >= 1500 & peak_labread <= 7499.9 ~ "100-499 times ULN",
      peak_labread >= 7500 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
ldiclo_hb_full_filtered_grouped
```


```{r}
ldiclo_labvalue_group_count <- ldiclo_hb_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
ldiclo_labvalue_group_count
```


```{r}
ldiclo_labvalue_group_count$Labvalue_Group <-  factor(ldiclo_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
ldiclo_labvalue_dimension_of_peak_hb <- ldiclo_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = ldiclo_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver diclo_labvalue dimension of peak-hb", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/hb_ldiclo_labvalue_dimension_of_peak_hb.svg", height = 8, width = 6.5)
print(ldiclo_labvalue_dimension_of_peak_hb)
dev.off()
```



#### Leber_Ibu ############

```{r}

libu_hb_full_filtered <- hb_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Ibu")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
libu_hb_full_filtered_grouped <- libu_hb_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 14.9 ~ "within normal reference",
      peak_labread >= 15 & peak_labread <= 44.9 ~ "1-2 times ULN",
      peak_labread >= 45 & peak_labread <= 89.9 ~ "3-5 times ULN",
      peak_labread >= 90 & peak_labread <= 149.9 ~ "6-9 times ULN",
      peak_labread >= 150 & peak_labread <= 749.9 ~ "10-49 times ULN",
      peak_labread >= 750 & peak_labread <= 1499.9 ~ "50-99 times ULN",
      peak_labread >= 1500 & peak_labread <= 7499.9 ~ "100-499 times ULN",
      peak_labread >= 7500 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
libu_labvalue_group_count <- libu_hb_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
libu_labvalue_group_count$Labvalue_Group <-  factor(libu_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
libu_labvalue_dimension_of_peak_hb <-libu_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = libu_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver ibu_labvalue dimension of peak-hb", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/hb_libu_labvalue_dimension_of_peak_hb.svg", height = 8, width = 6.5)
print(libu_labvalue_dimension_of_peak_hb)
dev.off()
```



#### Leber_Para ############

```{r}

lpara_hb_full_filtered <- hb_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Para")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
lpara_hb_full_filtered_grouped <- lpara_hb_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 14.9 ~ "within normal reference",
      peak_labread >= 15 & peak_labread <= 44.9 ~ "1-2 times ULN",
      peak_labread >= 45 & peak_labread <= 89.9 ~ "3-5 times ULN",
      peak_labread >= 90 & peak_labread <= 149.9 ~ "6-9 times ULN",
      peak_labread >= 150 & peak_labread <= 749.9 ~ "10-49 times ULN",
      peak_labread >= 750 & peak_labread <= 1499.9 ~ "50-99 times ULN",
      peak_labread >= 1500 & peak_labread <= 7499.9 ~ "100-499 times ULN",
      peak_labread >= 7500 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
lpara_labvalue_group_count <- lpara_hb_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
lpara_labvalue_group_count$Labvalue_Group <-  factor(lpara_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
lpara_labvalue_dimension_of_peak_hb <- lpara_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = lpara_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver_para labvalue dimension of peak-hb", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  ) #-> liver_para_labvalue_dimension_of_peak_hb


dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/hb_lpara_labvalue_dimension_of_peak_hb.svg", height = 8, width = 6.5)
print(lpara_labvalue_dimension_of_peak_hb)
dev.off()


```


### total_bilirubin #####################################################################

```{r}

total_bilirubin_full_filtered <- total_bilirubin_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
total_bilirubin_full_filtered
```

```{r}
total_bilirubin_full_filtered_grouped <- total_bilirubin_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 0.499 ~ "within normal reference",
      peak_labread >= 0.5 & peak_labread <= 1.499 ~ "1-2 times ULN",
      peak_labread >= 1.5 & peak_labread <= 2.99 ~ "3-5 times ULN",
      peak_labread >= 3 & peak_labread <= 4.99 ~ "6-9 times ULN",
      peak_labread >= 5 & peak_labread <= 24.99 ~ "10-49 times ULN",
      peak_labread >= 25 & peak_labread <= 49.99 ~ "50-99 times ULN",
      peak_labread >= 50 & peak_labread <= 249.99 ~ "100-499 times ULN",
      peak_labread >= 250 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
total_bilirubin_full_filtered_grouped
```




####labreads per Group count###
```{r}
labvalue_group_count <- total_bilirubin_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
labvalue_group_count
```



```{r}
labvalue_group_count$Labvalue_Group <-  factor(labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
all_patients_labvalue_dimension_of_peak_total_bilirubin <- labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "all patients_labvalue dimension of peak-total_bilirubin", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/total_bilirubin_all_patients_labvalue_dimension_of_peak_total_bilirubin.svg", height = 8, width = 6.5)
print(all_patients_labvalue_dimension_of_peak_total_bilirubin)
dev.off()
```

#### Leber_Diclo ############

```{r}

ldiclo_total_bilirubin_full_filtered <- total_bilirubin_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Diclo")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
ldiclo_total_bilirubin_full_filtered
```


```{r}
ldiclo_total_bilirubin_full_filtered_grouped <- ldiclo_total_bilirubin_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 0.499 ~ "within normal reference",
      peak_labread >= 0.5 & peak_labread <= 1.499 ~ "1-2 times ULN",
      peak_labread >= 1.5 & peak_labread <= 2.99 ~ "3-5 times ULN",
      peak_labread >= 3 & peak_labread <= 4.99 ~ "6-9 times ULN",
      peak_labread >= 5 & peak_labread <= 24.99 ~ "10-49 times ULN",
      peak_labread >= 25 & peak_labread <= 49.99 ~ "50-99 times ULN",
      peak_labread >= 50 & peak_labread <= 249.99 ~ "100-499 times ULN",
      peak_labread >= 250 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
ldiclo_total_bilirubin_full_filtered_grouped
```


```{r}
ldiclo_labvalue_group_count <- ldiclo_total_bilirubin_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
ldiclo_labvalue_group_count
```


```{r}
ldiclo_labvalue_group_count$Labvalue_Group <-  factor(ldiclo_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
ldiclo_labvalue_dimension_of_peak_total_bilirubin <- ldiclo_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = ldiclo_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver diclo_labvalue dimension of peak-total_bilirubin", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/total_bilirubin_ldiclo_labvalue_dimension_of_peak_total_bilirubin.svg", height = 8, width = 6.5)
print(ldiclo_labvalue_dimension_of_peak_total_bilirubin)
dev.off()
```



#### Leber_Ibu ############

```{r}

libu_total_bilirubin_full_filtered <- total_bilirubin_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Ibu")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
libu_total_bilirubin_full_filtered_grouped <- libu_total_bilirubin_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 0.499 ~ "within normal reference",
      peak_labread >= 0.5 & peak_labread <= 1.499 ~ "1-2 times ULN",
      peak_labread >= 1.5 & peak_labread <= 2.99 ~ "3-5 times ULN",
      peak_labread >= 3 & peak_labread <= 4.99 ~ "6-9 times ULN",
      peak_labread >= 5 & peak_labread <= 24.99 ~ "10-49 times ULN",
      peak_labread >= 25 & peak_labread <= 49.99 ~ "50-99 times ULN",
      peak_labread >= 50 & peak_labread <= 249.99 ~ "100-499 times ULN",
      peak_labread >= 250 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
libu_labvalue_group_count <- libu_total_bilirubin_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
libu_labvalue_group_count$Labvalue_Group <-  factor(libu_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
libu_labvalue_dimension_of_peak_total_bilirubin <-libu_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = libu_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver ibu_labvalue dimension of peak-total_bilirubin", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/total_bilirubin_libu_labvalue_dimension_of_peak_total_bilirubin.svg", height = 8, width = 6.5)
print(libu_labvalue_dimension_of_peak_total_bilirubin)
dev.off()
```



#### Leber_Para ############

```{r}

lpara_total_bilirubin_full_filtered <- total_bilirubin_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Para")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
lpara_total_bilirubin_full_filtered_grouped <- lpara_total_bilirubin_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 0.499 ~ "within normal reference",
      peak_labread >= 0.5 & peak_labread <= 1.499 ~ "1-2 times ULN",
      peak_labread >= 1.5 & peak_labread <= 2.99 ~ "3-5 times ULN",
      peak_labread >= 3 & peak_labread <= 4.99 ~ "6-9 times ULN",
      peak_labread >= 5 & peak_labread <= 24.99 ~ "10-49 times ULN",
      peak_labread >= 25 & peak_labread <= 49.99 ~ "50-99 times ULN",
      peak_labread >= 50 & peak_labread <= 249.99 ~ "100-499 times ULN",
      peak_labread >= 250 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
lpara_labvalue_group_count <- lpara_total_bilirubin_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
lpara_labvalue_group_count$Labvalue_Group <-  factor(lpara_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
lpara_labvalue_dimension_of_peak_total_bilirubin <- lpara_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = lpara_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver_para labvalue dimension of peak-total_bilirubin", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  ) #-> liver_para_labvalue_dimension_of_peak_total_bilirubin


dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/total_bilirubin_lpara_labvalue_dimension_of_peak_total_bilirubin.svg", height = 8, width = 6.5)
print(lpara_labvalue_dimension_of_peak_total_bilirubin)
dev.off()


```

### direct_bilirubin #####################################################################

```{r}

direct_bilirubin_full_filtered <- direct_bilirubin_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
direct_bilirubin_full_filtered
```

```{r}
direct_bilirubin_full_filtered_grouped <- direct_bilirubin_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 0.1499 ~ "within normal reference",
      peak_labread >= 0.15 & peak_labread <= 0.4499 ~ "1-2 times ULN",
      peak_labread >= 0.45 & peak_labread <= 0.899 ~ "3-5 times ULN",
      peak_labread >= 0.9 & peak_labread <= 1.499 ~ "6-9 times ULN",
      peak_labread >= 1.5 & peak_labread <= 7.499 ~ "10-49 times ULN",
      peak_labread >= 7.5 & peak_labread <= 14.99 ~ "50-99 times ULN",
      peak_labread >= 15 & peak_labread <= 74.99 ~ "100-499 times ULN",
      peak_labread >= 75 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
direct_bilirubin_full_filtered_grouped
```




####labreads per Group count###
```{r}
labvalue_group_count <- direct_bilirubin_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
labvalue_group_count
```



```{r}
labvalue_group_count$Labvalue_Group <-  factor(labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
all_patients_labvalue_dimension_of_peak_direct_bilirubin <- labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "all patients_labvalue dimension of peak-direct_bilirubin", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/direct_bilirubin_all_patients_labvalue_dimension_of_peak_direct_bilirubin.svg", height = 8, width = 6.5)
print(all_patients_labvalue_dimension_of_peak_direct_bilirubin)
dev.off()
```

#### Leber_Diclo ############

```{r}

ldiclo_direct_bilirubin_full_filtered <- direct_bilirubin_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Diclo")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
ldiclo_direct_bilirubin_full_filtered
```


```{r}
ldiclo_direct_bilirubin_full_filtered_grouped <- ldiclo_direct_bilirubin_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 0.1499 ~ "within normal reference",
      peak_labread >= 0.15 & peak_labread <= 0.4499 ~ "1-2 times ULN",
      peak_labread >= 0.45 & peak_labread <= 0.899 ~ "3-5 times ULN",
      peak_labread >= 0.9 & peak_labread <= 1.499 ~ "6-9 times ULN",
      peak_labread >= 1.5 & peak_labread <= 7.499 ~ "10-49 times ULN",
      peak_labread >= 7.5 & peak_labread <= 14.99 ~ "50-99 times ULN",
      peak_labread >= 15 & peak_labread <= 74.99 ~ "100-499 times ULN",
      peak_labread >= 75 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
ldiclo_direct_bilirubin_full_filtered_grouped
```


```{r}
ldiclo_labvalue_group_count <- ldiclo_direct_bilirubin_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
ldiclo_labvalue_group_count
```


```{r}
ldiclo_labvalue_group_count$Labvalue_Group <-  factor(ldiclo_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
ldiclo_labvalue_dimension_of_peak_direct_bilirubin <- ldiclo_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = ldiclo_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver diclo_labvalue dimension of peak-direct_bilirubin", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/direct_bilirubin_ldiclo_labvalue_dimension_of_peak_direct_bilirubin.svg", height = 8, width = 6.5)
print(ldiclo_labvalue_dimension_of_peak_direct_bilirubin)
dev.off()
```



#### Leber_Ibu ############

```{r}

libu_direct_bilirubin_full_filtered <- direct_bilirubin_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Ibu")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
libu_direct_bilirubin_full_filtered_grouped <- libu_direct_bilirubin_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 0.1499 ~ "within normal reference",
      peak_labread >= 0.15 & peak_labread <= 0.4499 ~ "1-2 times ULN",
      peak_labread >= 0.45 & peak_labread <= 0.899 ~ "3-5 times ULN",
      peak_labread >= 0.9 & peak_labread <= 1.499 ~ "6-9 times ULN",
      peak_labread >= 1.5 & peak_labread <= 7.499 ~ "10-49 times ULN",
      peak_labread >= 7.5 & peak_labread <= 14.99 ~ "50-99 times ULN",
      peak_labread >= 15 & peak_labread <= 74.99 ~ "100-499 times ULN",
      peak_labread >= 75 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
libu_labvalue_group_count <- libu_direct_bilirubin_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
libu_labvalue_group_count$Labvalue_Group <-  factor(libu_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
libu_labvalue_dimension_of_peak_direct_bilirubin <-libu_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = libu_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver ibu_labvalue dimension of peak-direct_bilirubin", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/direct_bilirubin_libu_labvalue_dimension_of_peak_direct_bilirubin.svg", height = 8, width = 6.5)
print(libu_labvalue_dimension_of_peak_direct_bilirubin)
dev.off()
```



#### Leber_Para ############

```{r}

lpara_direct_bilirubin_full_filtered <- direct_bilirubin_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Para")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
lpara_direct_bilirubin_full_filtered_grouped <- lpara_direct_bilirubin_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 0.1499 ~ "within normal reference",
      peak_labread >= 0.15 & peak_labread <= 0.4499 ~ "1-2 times ULN",
      peak_labread >= 0.45 & peak_labread <= 0.899 ~ "3-5 times ULN",
      peak_labread >= 0.9 & peak_labread <= 1.499 ~ "6-9 times ULN",
      peak_labread >= 1.5 & peak_labread <= 7.499 ~ "10-49 times ULN",
      peak_labread >= 7.5 & peak_labread <= 14.99 ~ "50-99 times ULN",
      peak_labread >= 15 & peak_labread <= 74.99 ~ "100-499 times ULN",
      peak_labread >= 75 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
lpara_labvalue_group_count <- lpara_direct_bilirubin_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
lpara_labvalue_group_count$Labvalue_Group <-  factor(lpara_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
lpara_labvalue_dimension_of_peak_direct_bilirubin <- lpara_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = lpara_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver_para labvalue dimension of peak-direct_bilirubin", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  ) #-> liver_para_labvalue_dimension_of_peak_direct_bilirubin


dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/direct_bilirubin_lpara_labvalue_dimension_of_peak_direct_bilirubin.svg", height = 8, width = 6.5)
print(lpara_labvalue_dimension_of_peak_direct_bilirubin)
dev.off()


```


### gesamteiweiß #####################################################################

```{r}

gesamteiweiß_full_filtered <- gesamteiweiß_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
gesamteiweiß_full_filtered
```

```{r}
gesamteiweiß_full_filtered_grouped <- gesamteiweiß_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 7.49 ~ "within normal reference",
      peak_labread >= 7.5 & peak_labread <= 22.49 ~ "1-2 times ULN",
      peak_labread >= 22.5 & peak_labread <= 44.9 ~ "3-5 times ULN",
      peak_labread >= 45 & peak_labread <= 74.9 ~ "6-9 times ULN",
      peak_labread >= 75 & peak_labread <= 374.9 ~ "10-49 times ULN",
      peak_labread >= 375 & peak_labread <= 749.9 ~ "50-99 times ULN",
      peak_labread >= 750 & peak_labread <= 3749.9 ~ "100-499 times ULN",
      peak_labread >= 3750 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
gesamteiweiß_full_filtered_grouped
```




####labreads per Group count###
```{r}
labvalue_group_count <- gesamteiweiß_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
labvalue_group_count
```



```{r}
labvalue_group_count$Labvalue_Group <-  factor(labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
all_patients_labvalue_dimension_of_peak_gesamteiweiß <- labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "all patients_labvalue dimension of peak-gesamteiweiß", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/gesamteiweiß_all_patients_labvalue_dimension_of_peak_gesamteiweiß.svg", height = 8, width = 6.5)
print(all_patients_labvalue_dimension_of_peak_gesamteiweiß)
dev.off()
```

#### Leber_Diclo ############

```{r}

ldiclo_gesamteiweiß_full_filtered <- gesamteiweiß_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Diclo")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
ldiclo_gesamteiweiß_full_filtered
```


```{r}
ldiclo_gesamteiweiß_full_filtered_grouped <- ldiclo_gesamteiweiß_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 7.49 ~ "within normal reference",
      peak_labread >= 7.5 & peak_labread <= 22.49 ~ "1-2 times ULN",
      peak_labread >= 22.5 & peak_labread <= 44.9 ~ "3-5 times ULN",
      peak_labread >= 45 & peak_labread <= 74.9 ~ "6-9 times ULN",
      peak_labread >= 75 & peak_labread <= 374.9 ~ "10-49 times ULN",
      peak_labread >= 375 & peak_labread <= 749.9 ~ "50-99 times ULN",
      peak_labread >= 750 & peak_labread <= 3749.9 ~ "100-499 times ULN",
      peak_labread >= 3750 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
ldiclo_gesamteiweiß_full_filtered_grouped
```


```{r}
ldiclo_labvalue_group_count <- ldiclo_gesamteiweiß_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
ldiclo_labvalue_group_count
```


```{r}
ldiclo_labvalue_group_count$Labvalue_Group <-  factor(ldiclo_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
ldiclo_labvalue_dimension_of_peak_gesamteiweiß <- ldiclo_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = ldiclo_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver diclo_labvalue dimension of peak-gesamteiweiß", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/gesamteiweiß_ldiclo_labvalue_dimension_of_peak_gesamteiweiß.svg", height = 8, width = 6.5)
print(ldiclo_labvalue_dimension_of_peak_gesamteiweiß)
dev.off()
```



#### Leber_Ibu ############

```{r}

libu_gesamteiweiß_full_filtered <- gesamteiweiß_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Ibu")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
libu_gesamteiweiß_full_filtered_grouped <- libu_gesamteiweiß_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 7.49 ~ "within normal reference",
      peak_labread >= 7.5 & peak_labread <= 22.49 ~ "1-2 times ULN",
      peak_labread >= 22.5 & peak_labread <= 44.9 ~ "3-5 times ULN",
      peak_labread >= 45 & peak_labread <= 74.9 ~ "6-9 times ULN",
      peak_labread >= 75 & peak_labread <= 374.9 ~ "10-49 times ULN",
      peak_labread >= 375 & peak_labread <= 749.9 ~ "50-99 times ULN",
      peak_labread >= 750 & peak_labread <= 3749.9 ~ "100-499 times ULN",
      peak_labread >= 3750 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
libu_labvalue_group_count <- libu_gesamteiweiß_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
libu_labvalue_group_count$Labvalue_Group <-  factor(libu_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
libu_labvalue_dimension_of_peak_gesamteiweiß <-libu_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = libu_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver ibu_labvalue dimension of peak-gesamteiweiß", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/gesamteiweiß_libu_labvalue_dimension_of_peak_gesamteiweiß.svg", height = 8, width = 6.5)
print(libu_labvalue_dimension_of_peak_gesamteiweiß)
dev.off()
```



#### Leber_Para ############

```{r}

lpara_gesamteiweiß_full_filtered <- gesamteiweiß_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Para")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
lpara_gesamteiweiß_full_filtered_grouped <- lpara_gesamteiweiß_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 7.49 ~ "within normal reference",
      peak_labread >= 7.5 & peak_labread <= 22.49 ~ "1-2 times ULN",
      peak_labread >= 22.5 & peak_labread <= 44.9 ~ "3-5 times ULN",
      peak_labread >= 45 & peak_labread <= 74.9 ~ "6-9 times ULN",
      peak_labread >= 75 & peak_labread <= 374.9 ~ "10-49 times ULN",
      peak_labread >= 375 & peak_labread <= 749.9 ~ "50-99 times ULN",
      peak_labread >= 750 & peak_labread <= 3749.9 ~ "100-499 times ULN",
      peak_labread >= 3750 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
lpara_labvalue_group_count <- lpara_gesamteiweiß_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
lpara_labvalue_group_count$Labvalue_Group <-  factor(lpara_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
lpara_labvalue_dimension_of_peak_gesamteiweiß <- lpara_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = lpara_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver_para labvalue dimension of peak-gesamteiweiß", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  ) #-> liver_para_labvalue_dimension_of_peak_gesamteiweiß


dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/gesamteiweiß_lpara_labvalue_dimension_of_peak_gesamteiweiß.svg", height = 8, width = 6.5)
print(lpara_labvalue_dimension_of_peak_gesamteiweiß)
dev.off()


```

### kreatinin #####################################################################

```{r}

kreatinin_full_filtered <- kreatinin_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
kreatinin_full_filtered
```

```{r}
kreatinin_full_filtered_grouped <- kreatinin_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 0.99 ~ "within normal reference",
      peak_labread >= 1 & peak_labread <= 2.99 ~ "1-2 times ULN",
      peak_labread >= 3 & peak_labread <= 5.99 ~ "3-5 times ULN",
      peak_labread >= 6 & peak_labread <= 9.99 ~ "6-9 times ULN",
      peak_labread >= 10 & peak_labread <= 49.99 ~ "10-49 times ULN",
      peak_labread >= 50 & peak_labread <= 99.99 ~ "50-99 times ULN",
      peak_labread >= 100 & peak_labread <= 499.99 ~ "100-499 times ULN",
      peak_labread >= 500 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
kreatinin_full_filtered_grouped
```




####labreads per Group count###
```{r}
labvalue_group_count <- kreatinin_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
labvalue_group_count
```



```{r}
labvalue_group_count$Labvalue_Group <-  factor(labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
all_patients_labvalue_dimension_of_peak_kreatinin <- labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "all patients_labvalue dimension of peak-kreatinin", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/kreatinin_all_patients_labvalue_dimension_of_peak_kreatinin.svg", height = 8, width = 6.5)
print(all_patients_labvalue_dimension_of_peak_kreatinin)
dev.off()
```

#### Leber_Diclo ############

```{r}

ldiclo_kreatinin_full_filtered <- kreatinin_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Diclo")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
ldiclo_kreatinin_full_filtered
```


```{r}
ldiclo_kreatinin_full_filtered_grouped <- ldiclo_kreatinin_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 0.99 ~ "within normal reference",
      peak_labread >= 1 & peak_labread <= 2.99 ~ "1-2 times ULN",
      peak_labread >= 3 & peak_labread <= 5.99 ~ "3-5 times ULN",
      peak_labread >= 6 & peak_labread <= 9.99 ~ "6-9 times ULN",
      peak_labread >= 10 & peak_labread <= 49.99 ~ "10-49 times ULN",
      peak_labread >= 50 & peak_labread <= 99.99 ~ "50-99 times ULN",
      peak_labread >= 100 & peak_labread <= 499.99 ~ "100-499 times ULN",
      peak_labread >= 500 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
ldiclo_kreatinin_full_filtered_grouped
```


```{r}
ldiclo_labvalue_group_count <- ldiclo_kreatinin_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
ldiclo_labvalue_group_count
```


```{r}
ldiclo_labvalue_group_count$Labvalue_Group <-  factor(ldiclo_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
ldiclo_labvalue_dimension_of_peak_kreatinin <- ldiclo_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = ldiclo_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver diclo_labvalue dimension of peak-kreatinin", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/kreatinin_ldiclo_labvalue_dimension_of_peak_kreatinin.svg", height = 8, width = 6.5)
print(ldiclo_labvalue_dimension_of_peak_kreatinin)
dev.off()
```



#### Leber_Ibu ############

```{r}

libu_kreatinin_full_filtered <- kreatinin_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Ibu")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
libu_kreatinin_full_filtered_grouped <- libu_kreatinin_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 0.99 ~ "within normal reference",
      peak_labread >= 1 & peak_labread <= 2.99 ~ "1-2 times ULN",
      peak_labread >= 3 & peak_labread <= 5.99 ~ "3-5 times ULN",
      peak_labread >= 6 & peak_labread <= 9.99 ~ "6-9 times ULN",
      peak_labread >= 10 & peak_labread <= 49.99 ~ "10-49 times ULN",
      peak_labread >= 50 & peak_labread <= 99.99 ~ "50-99 times ULN",
      peak_labread >= 100 & peak_labread <= 499.99 ~ "100-499 times ULN",
      peak_labread >= 500 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
libu_labvalue_group_count <- libu_kreatinin_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
libu_labvalue_group_count$Labvalue_Group <-  factor(libu_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
libu_labvalue_dimension_of_peak_kreatinin <-libu_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = libu_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver ibu_labvalue dimension of peak-kreatinin", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/kreatinin_libu_labvalue_dimension_of_peak_kreatinin.svg", height = 8, width = 6.5)
print(libu_labvalue_dimension_of_peak_kreatinin)
dev.off()
```



#### Leber_Para ############

```{r}

lpara_kreatinin_full_filtered <- kreatinin_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Para")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
lpara_kreatinin_full_filtered_grouped <- lpara_kreatinin_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 0.99 ~ "within normal reference",
      peak_labread >= 1 & peak_labread <= 2.99 ~ "1-2 times ULN",
      peak_labread >= 3 & peak_labread <= 5.99 ~ "3-5 times ULN",
      peak_labread >= 6 & peak_labread <= 9.99 ~ "6-9 times ULN",
      peak_labread >= 10 & peak_labread <= 49.99 ~ "10-49 times ULN",
      peak_labread >= 50 & peak_labread <= 99.99 ~ "50-99 times ULN",
      peak_labread >= 100 & peak_labread <= 499.99 ~ "100-499 times ULN",
      peak_labread >= 500 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
lpara_labvalue_group_count <- lpara_kreatinin_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
lpara_labvalue_group_count$Labvalue_Group <-  factor(lpara_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
lpara_labvalue_dimension_of_peak_kreatinin <- lpara_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = lpara_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver_para labvalue dimension of peak-kreatinin", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  ) #-> liver_para_labvalue_dimension_of_peak_kreatinin


dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/kreatinin_lpara_labvalue_dimension_of_peak_kreatinin.svg", height = 8, width = 6.5)
print(lpara_labvalue_dimension_of_peak_kreatinin)
dev.off()


```


### crp #####################################################################

```{r}

crp_full_filtered <- crp_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
crp_full_filtered
```

```{r}
crp_full_filtered_grouped <- crp_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 7.49 ~ "within normal reference",
      peak_labread >= 7.5 & peak_labread <= 22.49 ~ "1-2 times ULN",
      peak_labread >= 22.5 & peak_labread <= 44.9 ~ "3-5 times ULN",
      peak_labread >= 45 & peak_labread <= 74.9 ~ "6-9 times ULN",
      peak_labread >= 75 & peak_labread <= 374.9 ~ "10-49 times ULN",
      peak_labread >= 375 & peak_labread <= 749.9 ~ "50-99 times ULN",
      peak_labread >= 750 & peak_labread <= 3749.9 ~ "100-499 times ULN",
      peak_labread >= 3750 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
crp_full_filtered_grouped
```




####labreads per Group count###
```{r}
labvalue_group_count <- crp_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
labvalue_group_count
```



```{r}
labvalue_group_count$Labvalue_Group <-  factor(labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
all_patients_labvalue_dimension_of_peak_crp <- labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "all patients_labvalue dimension of peak-crp", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/crp_all_patients_labvalue_dimension_of_peak_crp.svg", height = 8, width = 6.5)
print(all_patients_labvalue_dimension_of_peak_crp)
dev.off()
```

#### Leber_Diclo ############

```{r}

ldiclo_crp_full_filtered <- crp_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Diclo")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
ldiclo_crp_full_filtered
```


```{r}
ldiclo_crp_full_filtered_grouped <- ldiclo_crp_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 7.49 ~ "within normal reference",
      peak_labread >= 7.5 & peak_labread <= 22.49 ~ "1-2 times ULN",
      peak_labread >= 22.5 & peak_labread <= 44.9 ~ "3-5 times ULN",
      peak_labread >= 45 & peak_labread <= 74.9 ~ "6-9 times ULN",
      peak_labread >= 75 & peak_labread <= 374.9 ~ "10-49 times ULN",
      peak_labread >= 375 & peak_labread <= 749.9 ~ "50-99 times ULN",
      peak_labread >= 750 & peak_labread <= 3749.9 ~ "100-499 times ULN",
      peak_labread >= 3750 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
ldiclo_crp_full_filtered_grouped
```


```{r}
ldiclo_labvalue_group_count <- ldiclo_crp_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
ldiclo_labvalue_group_count
```


```{r}
ldiclo_labvalue_group_count$Labvalue_Group <-  factor(ldiclo_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
ldiclo_labvalue_dimension_of_peak_crp <- ldiclo_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = ldiclo_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver diclo_labvalue dimension of peak-crp", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/crp_ldiclo_labvalue_dimension_of_peak_crp.svg", height = 8, width = 6.5)
print(ldiclo_labvalue_dimension_of_peak_crp)
dev.off()
```



#### Leber_Ibu ############

```{r}

libu_crp_full_filtered <- crp_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Ibu")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
libu_crp_full_filtered_grouped <- libu_crp_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 7.49 ~ "within normal reference",
      peak_labread >= 7.5 & peak_labread <= 22.49 ~ "1-2 times ULN",
      peak_labread >= 22.5 & peak_labread <= 44.9 ~ "3-5 times ULN",
      peak_labread >= 45 & peak_labread <= 74.9 ~ "6-9 times ULN",
      peak_labread >= 75 & peak_labread <= 374.9 ~ "10-49 times ULN",
      peak_labread >= 375 & peak_labread <= 749.9 ~ "50-99 times ULN",
      peak_labread >= 750 & peak_labread <= 3749.9 ~ "100-499 times ULN",
      peak_labread >= 3750 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
libu_labvalue_group_count <- libu_crp_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
libu_labvalue_group_count$Labvalue_Group <-  factor(libu_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
libu_labvalue_dimension_of_peak_crp <-libu_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = libu_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver ibu_labvalue dimension of peak-crp", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/crp_libu_labvalue_dimension_of_peak_crp.svg", height = 8, width = 6.5)
print(libu_labvalue_dimension_of_peak_crp)
dev.off()
```



#### Leber_Para ############

```{r}

lpara_crp_full_filtered <- crp_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Para")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
lpara_crp_full_filtered_grouped <- lpara_crp_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 7.49 ~ "within normal reference",
      peak_labread >= 7.5 & peak_labread <= 22.49 ~ "1-2 times ULN",
      peak_labread >= 22.5 & peak_labread <= 44.9 ~ "3-5 times ULN",
      peak_labread >= 45 & peak_labread <= 74.9 ~ "6-9 times ULN",
      peak_labread >= 75 & peak_labread <= 374.9 ~ "10-49 times ULN",
      peak_labread >= 375 & peak_labread <= 749.9 ~ "50-99 times ULN",
      peak_labread >= 750 & peak_labread <= 3749.9 ~ "100-499 times ULN",
      peak_labread >= 3750 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
lpara_labvalue_group_count <- lpara_crp_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
lpara_labvalue_group_count$Labvalue_Group <-  factor(lpara_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
lpara_labvalue_dimension_of_peak_crp <- lpara_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = lpara_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver_para labvalue dimension of peak-crp", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  ) #-> liver_para_labvalue_dimension_of_peak_crp


dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/crp_lpara_labvalue_dimension_of_peak_crp.svg", height = 8, width = 6.5)
print(lpara_labvalue_dimension_of_peak_crp)
dev.off()


```


### harnstoff #####################################################################

```{r}

harnstoff_full_filtered <- harnstoff_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
harnstoff_full_filtered
```

```{r}
harnstoff_full_filtered_grouped <- harnstoff_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 32.9 ~ "within normal reference",
      peak_labread >= 33 & peak_labread <= 98.9 ~ "1-2 times ULN",
      peak_labread >= 99 & peak_labread <= 197.9 ~ "3-5 times ULN",
      peak_labread >= 198 & peak_labread <= 329.9 ~ "6-9 times ULN",
      peak_labread >= 330 & peak_labread <= 1649.9 ~ "10-49 times ULN",
      peak_labread >= 1650 & peak_labread <= 3299.9 ~ "50-99 times ULN",
      peak_labread >= 3300 & peak_labread <= 16499.9 ~ "100-499 times ULN",
      peak_labread >= 16500 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
harnstoff_full_filtered_grouped
```




####labreads per Group count###
```{r}
labvalue_group_count <- harnstoff_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
labvalue_group_count
```



```{r}
labvalue_group_count$Labvalue_Group <-  factor(labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
all_patients_labvalue_dimension_of_peak_harnstoff <- labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "all patients_labvalue dimension of peak-harnstoff", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/harnstoff_all_patients_labvalue_dimension_of_peak_harnstoff.svg", height = 8, width = 6.5)
print(all_patients_labvalue_dimension_of_peak_harnstoff)
dev.off()
```

#### Leber_Diclo ############

```{r}

ldiclo_harnstoff_full_filtered <- harnstoff_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Diclo")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
ldiclo_harnstoff_full_filtered
```


```{r}
ldiclo_harnstoff_full_filtered_grouped <- ldiclo_harnstoff_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 32.9 ~ "within normal reference",
      peak_labread >= 33 & peak_labread <= 98.9 ~ "1-2 times ULN",
      peak_labread >= 99 & peak_labread <= 197.9 ~ "3-5 times ULN",
      peak_labread >= 198 & peak_labread <= 329.9 ~ "6-9 times ULN",
      peak_labread >= 330 & peak_labread <= 1649.9 ~ "10-49 times ULN",
      peak_labread >= 1650 & peak_labread <= 3299.9 ~ "50-99 times ULN",
      peak_labread >= 3300 & peak_labread <= 16499.9 ~ "100-499 times ULN",
      peak_labread >= 16500 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
ldiclo_harnstoff_full_filtered_grouped
```


```{r}
ldiclo_labvalue_group_count <- ldiclo_harnstoff_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
ldiclo_labvalue_group_count
```


```{r}
ldiclo_labvalue_group_count$Labvalue_Group <-  factor(ldiclo_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
ldiclo_labvalue_dimension_of_peak_harnstoff <- ldiclo_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = ldiclo_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver diclo_labvalue dimension of peak-harnstoff", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/harnstoff_ldiclo_labvalue_dimension_of_peak_harnstoff.svg", height = 8, width = 6.5)
print(ldiclo_labvalue_dimension_of_peak_harnstoff)
dev.off()
```



#### Leber_Ibu ############

```{r}

libu_harnstoff_full_filtered <- harnstoff_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Ibu")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
libu_harnstoff_full_filtered_grouped <- libu_harnstoff_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 32.9 ~ "within normal reference",
      peak_labread >= 33 & peak_labread <= 98.9 ~ "1-2 times ULN",
      peak_labread >= 99 & peak_labread <= 197.9 ~ "3-5 times ULN",
      peak_labread >= 198 & peak_labread <= 329.9 ~ "6-9 times ULN",
      peak_labread >= 330 & peak_labread <= 1649.9 ~ "10-49 times ULN",
      peak_labread >= 1650 & peak_labread <= 3299.9 ~ "50-99 times ULN",
      peak_labread >= 3300 & peak_labread <= 16499.9 ~ "100-499 times ULN",
      peak_labread >= 16500 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
libu_labvalue_group_count <- libu_harnstoff_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
libu_labvalue_group_count$Labvalue_Group <-  factor(libu_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
libu_labvalue_dimension_of_peak_harnstoff <-libu_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = libu_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver ibu_labvalue dimension of peak-harnstoff", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/harnstoff_libu_labvalue_dimension_of_peak_harnstoff.svg", height = 8, width = 6.5)
print(libu_labvalue_dimension_of_peak_harnstoff)
dev.off()
```



#### Leber_Para ############

```{r}

lpara_harnstoff_full_filtered <- harnstoff_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Para")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
lpara_harnstoff_full_filtered_grouped <- lpara_harnstoff_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 32.9 ~ "within normal reference",
      peak_labread >= 33 & peak_labread <= 98.9 ~ "1-2 times ULN",
      peak_labread >= 99 & peak_labread <= 197.9 ~ "3-5 times ULN",
      peak_labread >= 198 & peak_labread <= 329.9 ~ "6-9 times ULN",
      peak_labread >= 330 & peak_labread <= 1649.9 ~ "10-49 times ULN",
      peak_labread >= 1650 & peak_labread <= 3299.9 ~ "50-99 times ULN",
      peak_labread >= 3300 & peak_labread <= 16499.9 ~ "100-499 times ULN",
      peak_labread >= 16500 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
lpara_labvalue_group_count <- lpara_harnstoff_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
lpara_labvalue_group_count$Labvalue_Group <-  factor(lpara_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
lpara_labvalue_dimension_of_peak_harnstoff <- lpara_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = lpara_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver_para labvalue dimension of peak-harnstoff", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  ) #-> liver_para_labvalue_dimension_of_peak_harnstoff


dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/harnstoff_lpara_labvalue_dimension_of_peak_harnstoff.svg", height = 8, width = 6.5)
print(lpara_labvalue_dimension_of_peak_harnstoff)
dev.off()


```


### erythrozyten #####################################################################

```{r}

erythrozyten_full_filtered <- erythrozyten_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
erythrozyten_full_filtered
```

```{r}
erythrozyten_full_filtered_grouped <- erythrozyten_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 4749999 ~ "within normal reference",
      peak_labread >= 4750000 & peak_labread <= 14249999 ~ "1-2 times ULN",
      peak_labread >= 14250000 & peak_labread <= 28499999 ~ "3-5 times ULN",
      peak_labread >= 28500000 & peak_labread <= 47499999 ~ "6-9 times ULN",
      peak_labread >= 47500000 & peak_labread <= 237499999 ~ "10-49 times ULN",
      peak_labread >= 237500000 & peak_labread <= 474999999 ~ "50-99 times ULN",
      peak_labread >= 475000000 & peak_labread <= 2374999999 ~ "100-499 times ULN",
      peak_labread >= 2375000000 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
erythrozyten_full_filtered_grouped
```




####labreads per Group count###
```{r}
labvalue_group_count <- erythrozyten_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
labvalue_group_count
```



```{r}
labvalue_group_count$Labvalue_Group <-  factor(labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
all_patients_labvalue_dimension_of_peak_erythrozyten <- labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "all patients_labvalue dimension of peak-erythrozyten", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/erythrozyten_all_patients_labvalue_dimension_of_peak_erythrozyten.svg", height = 8, width = 6.5)
print(all_patients_labvalue_dimension_of_peak_erythrozyten)
dev.off()
```

#### Leber_Diclo ############

```{r}

ldiclo_erythrozyten_full_filtered <- erythrozyten_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Diclo")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
ldiclo_erythrozyten_full_filtered
```


```{r}
ldiclo_erythrozyten_full_filtered_grouped <- ldiclo_erythrozyten_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 4749999 ~ "within normal reference",
      peak_labread >= 4750000 & peak_labread <= 14249999 ~ "1-2 times ULN",
      peak_labread >= 14250000 & peak_labread <= 28499999 ~ "3-5 times ULN",
      peak_labread >= 28500000 & peak_labread <= 47499999 ~ "6-9 times ULN",
      peak_labread >= 47500000 & peak_labread <= 237499999 ~ "10-49 times ULN",
      peak_labread >= 237500000 & peak_labread <= 474999999 ~ "50-99 times ULN",
      peak_labread >= 475000000 & peak_labread <= 2374999999 ~ "100-499 times ULN",
      peak_labread >= 2375000000 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
ldiclo_erythrozyten_full_filtered_grouped
```


```{r}
ldiclo_labvalue_group_count <- ldiclo_erythrozyten_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
ldiclo_labvalue_group_count
```


```{r}
ldiclo_labvalue_group_count$Labvalue_Group <-  factor(ldiclo_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
ldiclo_labvalue_dimension_of_peak_erythrozyten <- ldiclo_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = ldiclo_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver diclo_labvalue dimension of peak-erythrozyten", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/erythrozyten_ldiclo_labvalue_dimension_of_peak_erythrozyten.svg", height = 8, width = 6.5)
print(ldiclo_labvalue_dimension_of_peak_erythrozyten)
dev.off()
```



#### Leber_Ibu ############

```{r}

libu_erythrozyten_full_filtered <- erythrozyten_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Ibu")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
libu_erythrozyten_full_filtered_grouped <- libu_erythrozyten_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 4749999 ~ "within normal reference",
      peak_labread >= 4750000 & peak_labread <= 14249999 ~ "1-2 times ULN",
      peak_labread >= 14250000 & peak_labread <= 28499999 ~ "3-5 times ULN",
      peak_labread >= 28500000 & peak_labread <= 47499999 ~ "6-9 times ULN",
      peak_labread >= 47500000 & peak_labread <= 237499999 ~ "10-49 times ULN",
      peak_labread >= 237500000 & peak_labread <= 474999999 ~ "50-99 times ULN",
      peak_labread >= 475000000 & peak_labread <= 2374999999 ~ "100-499 times ULN",
      peak_labread >= 2375000000 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
libu_labvalue_group_count <- libu_erythrozyten_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
libu_labvalue_group_count$Labvalue_Group <-  factor(libu_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
libu_labvalue_dimension_of_peak_erythrozyten <-libu_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = libu_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver ibu_labvalue dimension of peak-erythrozyten", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/erythrozyten_libu_labvalue_dimension_of_peak_erythrozyten.svg", height = 8, width = 6.5)
print(libu_labvalue_dimension_of_peak_erythrozyten)
dev.off()
```



#### Leber_Para ############

```{r}

lpara_erythrozyten_full_filtered <- erythrozyten_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Para")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
lpara_erythrozyten_full_filtered_grouped <- lpara_erythrozyten_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 4749999 ~ "within normal reference",
      peak_labread >= 4750000 & peak_labread <= 14249999 ~ "1-2 times ULN",
      peak_labread >= 14250000 & peak_labread <= 28499999 ~ "3-5 times ULN",
      peak_labread >= 28500000 & peak_labread <= 47499999 ~ "6-9 times ULN",
      peak_labread >= 47500000 & peak_labread <= 237499999 ~ "10-49 times ULN",
      peak_labread >= 237500000 & peak_labread <= 474999999 ~ "50-99 times ULN",
      peak_labread >= 475000000 & peak_labread <= 2374999999 ~ "100-499 times ULN",
      peak_labread >= 2375000000 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
lpara_labvalue_group_count <- lpara_erythrozyten_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
lpara_labvalue_group_count$Labvalue_Group <-  factor(lpara_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
lpara_labvalue_dimension_of_peak_erythrozyten <- lpara_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = lpara_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver_para labvalue dimension of peak-erythrozyten", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  ) #-> liver_para_labvalue_dimension_of_peak_erythrozyten


dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/erythrozyten_lpara_labvalue_dimension_of_peak_erythrozyten.svg", height = 8, width = 6.5)
print(lpara_labvalue_dimension_of_peak_erythrozyten)
dev.off()


```


### thrombozyten #####################################################################

```{r}

thrombozyten_full_filtered <- thrombozyten_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
thrombozyten_full_filtered
```

```{r}
thrombozyten_full_filtered_grouped <- thrombozyten_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 224999 ~ "within normal reference",
      peak_labread >= 225000 & peak_labread <= 674999 ~ "1-2 times ULN",
      peak_labread >= 675000 & peak_labread <= 1349999 ~ "3-5 times ULN",
      peak_labread >= 1350000 & peak_labread < 2249999 ~ "6-9 times ULN",
      peak_labread >= 2250000 & peak_labread <= 11249999 ~ "10-49 times ULN",
      peak_labread >= 11250000 & peak_labread <= 22499999 ~ "50-99 times ULN",
      peak_labread >= 22500000 & peak_labread <= 112499999 ~ "100-499 times ULN",
      peak_labread >= 112500000 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
thrombozyten_full_filtered_grouped
```




####labreads per Group count###
```{r}
labvalue_group_count <- thrombozyten_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
labvalue_group_count
```



```{r}
labvalue_group_count$Labvalue_Group <-  factor(labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
all_patients_labvalue_dimension_of_peak_thrombozyten <- labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "all patients_labvalue dimension of peak-thrombozyten", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/thrombozyten_all_patients_labvalue_dimension_of_peak_thrombozyten.svg", height = 8, width = 6.5)
print(all_patients_labvalue_dimension_of_peak_thrombozyten)
dev.off()
```

#### Leber_Diclo ############

```{r}

ldiclo_thrombozyten_full_filtered <- thrombozyten_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Diclo")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
ldiclo_thrombozyten_full_filtered
```


```{r}
ldiclo_thrombozyten_full_filtered_grouped <- ldiclo_thrombozyten_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 224999 ~ "within normal reference",
      peak_labread >= 225000 & peak_labread <= 674999 ~ "1-2 times ULN",
      peak_labread >= 675000 & peak_labread <= 1349999 ~ "3-5 times ULN",
      peak_labread >= 1350000 & peak_labread < 2249999 ~ "6-9 times ULN",
      peak_labread >= 2250000 & peak_labread <= 11249999 ~ "10-49 times ULN",
      peak_labread >= 11250000 & peak_labread <= 22499999 ~ "50-99 times ULN",
      peak_labread >= 22500000 & peak_labread <= 112499999 ~ "100-499 times ULN",
      peak_labread >= 112500000 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
ldiclo_thrombozyten_full_filtered_grouped
```


```{r}
ldiclo_labvalue_group_count <- ldiclo_thrombozyten_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
ldiclo_labvalue_group_count
```


```{r}
ldiclo_labvalue_group_count$Labvalue_Group <-  factor(ldiclo_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
ldiclo_labvalue_dimension_of_peak_thrombozyten <- ldiclo_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = ldiclo_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver diclo_labvalue dimension of peak-thrombozyten", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/thrombozyten_ldiclo_labvalue_dimension_of_peak_thrombozyten.svg", height = 8, width = 6.5)
print(ldiclo_labvalue_dimension_of_peak_thrombozyten)
dev.off()
```



#### Leber_Ibu ############

```{r}

libu_thrombozyten_full_filtered <- thrombozyten_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Ibu")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
libu_thrombozyten_full_filtered_grouped <- libu_thrombozyten_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 224999 ~ "within normal reference",
      peak_labread >= 225000 & peak_labread <= 674999 ~ "1-2 times ULN",
      peak_labread >= 675000 & peak_labread <= 1349999 ~ "3-5 times ULN",
      peak_labread >= 1350000 & peak_labread < 2249999 ~ "6-9 times ULN",
      peak_labread >= 2250000 & peak_labread <= 11249999 ~ "10-49 times ULN",
      peak_labread >= 11250000 & peak_labread <= 22499999 ~ "50-99 times ULN",
      peak_labread >= 22500000 & peak_labread <= 112499999 ~ "100-499 times ULN",
      peak_labread >= 112500000 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
libu_labvalue_group_count <- libu_thrombozyten_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
libu_labvalue_group_count$Labvalue_Group <-  factor(libu_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
libu_labvalue_dimension_of_peak_thrombozyten <-libu_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = libu_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver ibu_labvalue dimension of peak-thrombozyten", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/thrombozyten_libu_labvalue_dimension_of_peak_thrombozyten.svg", height = 8, width = 6.5)
print(libu_labvalue_dimension_of_peak_thrombozyten)
dev.off()
```



#### Leber_Para ############

```{r}

lpara_thrombozyten_full_filtered <- thrombozyten_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Para")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
lpara_thrombozyten_full_filtered_grouped <- lpara_thrombozyten_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 224999 ~ "within normal reference",
      peak_labread >= 225000 & peak_labread <= 674999 ~ "1-2 times ULN",
      peak_labread >= 675000 & peak_labread <= 1349999 ~ "3-5 times ULN",
      peak_labread >= 1350000 & peak_labread < 2249999 ~ "6-9 times ULN",
      peak_labread >= 2250000 & peak_labread <= 11249999 ~ "10-49 times ULN",
      peak_labread >= 11250000 & peak_labread <= 22499999 ~ "50-99 times ULN",
      peak_labread >= 22500000 & peak_labread <= 112499999 ~ "100-499 times ULN",
      peak_labread >= 112500000 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
lpara_labvalue_group_count <- lpara_thrombozyten_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
lpara_labvalue_group_count$Labvalue_Group <-  factor(lpara_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
lpara_labvalue_dimension_of_peak_thrombozyten <- lpara_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = lpara_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver_para labvalue dimension of peak-thrombozyten", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  ) #-> liver_para_labvalue_dimension_of_peak_thrombozyten


dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/thrombozyten_lpara_labvalue_dimension_of_peak_thrombozyten.svg", height = 8, width = 6.5)
print(lpara_labvalue_dimension_of_peak_thrombozyten)
dev.off()


```


### hämatokrit #####################################################################

```{r}

hämatokrit_full_filtered <- hämatokrit_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
hämatokrit_full_filtered
```

```{r}
hämatokrit_full_filtered_grouped <- hämatokrit_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 39.9 ~ "within normal reference",
      peak_labread >= 40 & peak_labread <= 119.9 ~ "1-2 times ULN",
      peak_labread >= 120 & peak_labread <= 239.9 ~ "3-5 times ULN",
      peak_labread >= 240 & peak_labread <= 399.9 ~ "6-9 times ULN",
      peak_labread >= 400 & peak_labread <= 1999.9 ~ "10-49 times ULN",
      peak_labread >= 2000 & peak_labread <= 3999.9 ~ "50-99 times ULN",
      peak_labread >= 4000 & peak_labread <= 19999.9 ~ "100-499 times ULN",
      peak_labread >= 200000 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
hämatokrit_full_filtered_grouped
```




####labreads per Group count###
```{r}
labvalue_group_count <- hämatokrit_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
labvalue_group_count
```



```{r}
labvalue_group_count$Labvalue_Group <-  factor(labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
all_patients_labvalue_dimension_of_peak_hämatokrit <- labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "all patients_labvalue dimension of peak-hämatokrit", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/hämatokrit_all_patients_labvalue_dimension_of_peak_hämatokrit.svg", height = 8, width = 6.5)
print(all_patients_labvalue_dimension_of_peak_hämatokrit)
dev.off()
```

#### Leber_Diclo ############

```{r}

ldiclo_hämatokrit_full_filtered <- hämatokrit_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Diclo")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
ldiclo_hämatokrit_full_filtered
```


```{r}
ldiclo_hämatokrit_full_filtered_grouped <- ldiclo_hämatokrit_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 39.9 ~ "within normal reference",
      peak_labread >= 40 & peak_labread <= 119.9 ~ "1-2 times ULN",
      peak_labread >= 120 & peak_labread <= 239.9 ~ "3-5 times ULN",
      peak_labread >= 240 & peak_labread <= 399.9 ~ "6-9 times ULN",
      peak_labread >= 400 & peak_labread <= 1999.9 ~ "10-49 times ULN",
      peak_labread >= 2000 & peak_labread <= 3999.9 ~ "50-99 times ULN",
      peak_labread >= 4000 & peak_labread <= 19999.9 ~ "100-499 times ULN",
      peak_labread >= 200000 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
ldiclo_hämatokrit_full_filtered_grouped
```


```{r}
ldiclo_labvalue_group_count <- ldiclo_hämatokrit_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
ldiclo_labvalue_group_count
```


```{r}
ldiclo_labvalue_group_count$Labvalue_Group <-  factor(ldiclo_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
ldiclo_labvalue_dimension_of_peak_hämatokrit <- ldiclo_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = ldiclo_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver diclo_labvalue dimension of peak-hämatokrit", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/hämatokrit_ldiclo_labvalue_dimension_of_peak_hämatokrit.svg", height = 8, width = 6.5)
print(ldiclo_labvalue_dimension_of_peak_hämatokrit)
dev.off()
```



#### Leber_Ibu ############

```{r}

libu_hämatokrit_full_filtered <- hämatokrit_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Ibu")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
libu_hämatokrit_full_filtered_grouped <- libu_hämatokrit_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 39.9 ~ "within normal reference",
      peak_labread >= 40 & peak_labread <= 119.9 ~ "1-2 times ULN",
      peak_labread >= 120 & peak_labread <= 239.9 ~ "3-5 times ULN",
      peak_labread >= 240 & peak_labread <= 399.9 ~ "6-9 times ULN",
      peak_labread >= 400 & peak_labread <= 1999.9 ~ "10-49 times ULN",
      peak_labread >= 2000 & peak_labread <= 3999.9 ~ "50-99 times ULN",
      peak_labread >= 4000 & peak_labread <= 19999.9 ~ "100-499 times ULN",
      peak_labread >= 200000 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
libu_labvalue_group_count <- libu_hämatokrit_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
libu_labvalue_group_count$Labvalue_Group <-  factor(libu_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
libu_labvalue_dimension_of_peak_hämatokrit <-libu_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = libu_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver ibu_labvalue dimension of peak-hämatokrit", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/hämatokrit_libu_labvalue_dimension_of_peak_hämatokrit.svg", height = 8, width = 6.5)
print(libu_labvalue_dimension_of_peak_hämatokrit)
dev.off()
```



#### Leber_Para ############

```{r}

lpara_hämatokrit_full_filtered <- hämatokrit_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Para")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
lpara_hämatokrit_full_filtered_grouped <- lpara_hämatokrit_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 39.9 ~ "within normal reference",
      peak_labread >= 40 & peak_labread <= 119.9 ~ "1-2 times ULN",
      peak_labread >= 120 & peak_labread <= 239.9 ~ "3-5 times ULN",
      peak_labread >= 240 & peak_labread <= 399.9 ~ "6-9 times ULN",
      peak_labread >= 400 & peak_labread <= 1999.9 ~ "10-49 times ULN",
      peak_labread >= 2000 & peak_labread <= 3999.9 ~ "50-99 times ULN",
      peak_labread >= 4000 & peak_labread <= 19999.9 ~ "100-499 times ULN",
      peak_labread >= 200000 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
lpara_labvalue_group_count <- lpara_hämatokrit_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
lpara_labvalue_group_count$Labvalue_Group <-  factor(lpara_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
lpara_labvalue_dimension_of_peak_hämatokrit <- lpara_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = lpara_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver_para labvalue dimension of peak-hämatokrit", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  ) #-> liver_para_labvalue_dimension_of_peak_hämatokrit


dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/hämatokrit_lpara_labvalue_dimension_of_peak_hämatokrit.svg", height = 8, width = 6.5)
print(lpara_labvalue_dimension_of_peak_hämatokrit)
dev.off()


```


### natrium #####################################################################

```{r}

natrium_full_filtered <- natrium_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
natrium_full_filtered
```

```{r}
natrium_full_filtered_grouped <- natrium_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 282.9 ~ "within normal reference",
      peak_labread >= 283 & peak_labread <= 848.9 ~ "1-2 times ULN",
      peak_labread >= 849 & peak_labread <= 1697.9 ~ "3-5 times ULN",
      peak_labread >= 1698 & peak_labread <= 2829.9 ~ "6-9 times ULN",
      peak_labread >= 2830 & peak_labread <= 14149.9 ~ "10-49 times ULN",
      peak_labread >= 14150 & peak_labread <= 28299.9 ~ "50-99 times ULN",
      peak_labread >= 28300 & peak_labread <= 141499.9 ~ "100-499 times ULN",
      peak_labread >= 141500 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
natrium_full_filtered_grouped
```




####labreads per Group count###
```{r}
labvalue_group_count <- natrium_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
labvalue_group_count
```



```{r}
labvalue_group_count$Labvalue_Group <-  factor(labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
all_patients_labvalue_dimension_of_peak_natrium <- labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "all patients_labvalue dimension of peak-natrium", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/natrium_all_patients_labvalue_dimension_of_peak_natrium.svg", height = 8, width = 6.5)
print(all_patients_labvalue_dimension_of_peak_natrium)
dev.off()
```

#### Leber_Diclo ############

```{r}

ldiclo_natrium_full_filtered <- natrium_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Diclo")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
ldiclo_natrium_full_filtered
```


```{r}
ldiclo_natrium_full_filtered_grouped <- ldiclo_natrium_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 282.9 ~ "within normal reference",
      peak_labread >= 283 & peak_labread <= 848.9 ~ "1-2 times ULN",
      peak_labread >= 849 & peak_labread <= 1697.9 ~ "3-5 times ULN",
      peak_labread >= 1698 & peak_labread <= 2829.9 ~ "6-9 times ULN",
      peak_labread >= 2830 & peak_labread <= 14149.9 ~ "10-49 times ULN",
      peak_labread >= 14150 & peak_labread <= 28299.9 ~ "50-99 times ULN",
      peak_labread >= 28300 & peak_labread <= 141499.9 ~ "100-499 times ULN",
      peak_labread >= 141500 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
ldiclo_natrium_full_filtered_grouped
```


```{r}
ldiclo_labvalue_group_count <- ldiclo_natrium_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
ldiclo_labvalue_group_count
```


```{r}
ldiclo_labvalue_group_count$Labvalue_Group <-  factor(ldiclo_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
ldiclo_labvalue_dimension_of_peak_natrium <- ldiclo_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = ldiclo_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver diclo_labvalue dimension of peak-natrium", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/natrium_ldiclo_labvalue_dimension_of_peak_natrium.svg", height = 8, width = 6.5)
print(ldiclo_labvalue_dimension_of_peak_natrium)
dev.off()
```



#### Leber_Ibu ############

```{r}

libu_natrium_full_filtered <- natrium_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Ibu")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
libu_natrium_full_filtered_grouped <- libu_natrium_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 282.9 ~ "within normal reference",
      peak_labread >= 283 & peak_labread <= 848.9 ~ "1-2 times ULN",
      peak_labread >= 849 & peak_labread <= 1697.9 ~ "3-5 times ULN",
      peak_labread >= 1698 & peak_labread <= 2829.9 ~ "6-9 times ULN",
      peak_labread >= 2830 & peak_labread <= 14149.9 ~ "10-49 times ULN",
      peak_labread >= 14150 & peak_labread <= 28299.9 ~ "50-99 times ULN",
      peak_labread >= 28300 & peak_labread <= 141499.9 ~ "100-499 times ULN",
      peak_labread >= 141500 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
libu_labvalue_group_count <- libu_natrium_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
libu_labvalue_group_count$Labvalue_Group <-  factor(libu_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
libu_labvalue_dimension_of_peak_natrium <-libu_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = libu_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver ibu_labvalue dimension of peak-natrium", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/natrium_libu_labvalue_dimension_of_peak_natrium.svg", height = 8, width = 6.5)
print(libu_labvalue_dimension_of_peak_natrium)
dev.off()
```



#### Leber_Para ############

```{r}

lpara_natrium_full_filtered <- natrium_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Para")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
lpara_natrium_full_filtered_grouped <- lpara_natrium_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 282.9 ~ "within normal reference",
      peak_labread >= 283 & peak_labread <= 848.9 ~ "1-2 times ULN",
      peak_labread >= 849 & peak_labread <= 1697.9 ~ "3-5 times ULN",
      peak_labread >= 1698 & peak_labread <= 2829.9 ~ "6-9 times ULN",
      peak_labread >= 2830 & peak_labread <= 14149.9 ~ "10-49 times ULN",
      peak_labread >= 14150 & peak_labread <= 28299.9 ~ "50-99 times ULN",
      peak_labread >= 28300 & peak_labread <= 141499.9 ~ "100-499 times ULN",
      peak_labread >= 141500 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
lpara_labvalue_group_count <- lpara_natrium_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
lpara_labvalue_group_count$Labvalue_Group <-  factor(lpara_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
lpara_labvalue_dimension_of_peak_natrium <- lpara_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = lpara_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver_para labvalue dimension of peak-natrium", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  ) #-> liver_para_labvalue_dimension_of_peak_natrium


dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/natrium_lpara_labvalue_dimension_of_peak_natrium.svg", height = 8, width = 6.5)
print(lpara_labvalue_dimension_of_peak_natrium)
dev.off()


```


### kalium #####################################################################

```{r}

kalium_full_filtered <- kalium_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
kalium_full_filtered
```

```{r}
kalium_full_filtered_grouped <- kalium_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 4.39 ~ "within normal reference",
      peak_labread >= 4.4 & peak_labread <= 13.19 ~ "1-2 times ULN",
      peak_labread >= 13.2 & peak_labread <= 25.39 ~ "3-5 times ULN",
      peak_labread >= 26.4 & peak_labread <= 43.99 ~ "6-9 times ULN",
      peak_labread >= 44 & peak_labread <= 219.9 ~ "10-49 times ULN",
      peak_labread >= 220 & peak_labread <= 439.9 ~ "50-99 times ULN",
      peak_labread >= 440 & peak_labread <= 2221.9 ~ "100-499 times ULN",
      peak_labread >= 2222 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
kalium_full_filtered_grouped
```




####labreads per Group count###
```{r}
labvalue_group_count <- kalium_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
labvalue_group_count
```



```{r}
labvalue_group_count$Labvalue_Group <-  factor(labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
all_patients_labvalue_dimension_of_peak_kalium <- labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "all patients_labvalue dimension of peak-kalium", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/kalium_all_patients_labvalue_dimension_of_peak_kalium.svg", height = 8, width = 6.5)
print(all_patients_labvalue_dimension_of_peak_kalium)
dev.off()
```

#### Leber_Diclo ############

```{r}

ldiclo_kalium_full_filtered <- kalium_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Diclo")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
ldiclo_kalium_full_filtered
```


```{r}
ldiclo_kalium_full_filtered_grouped <- ldiclo_kalium_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 4.39 ~ "within normal reference",
      peak_labread >= 4.4 & peak_labread <= 13.19 ~ "1-2 times ULN",
      peak_labread >= 13.2 & peak_labread <= 25.39 ~ "3-5 times ULN",
      peak_labread >= 26.4 & peak_labread <= 43.99 ~ "6-9 times ULN",
      peak_labread >= 44 & peak_labread <= 219.9 ~ "10-49 times ULN",
      peak_labread >= 220 & peak_labread <= 439.9 ~ "50-99 times ULN",
      peak_labread >= 440 & peak_labread <= 2221.9 ~ "100-499 times ULN",
      peak_labread >= 2222 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
ldiclo_kalium_full_filtered_grouped
```


```{r}
ldiclo_labvalue_group_count <- ldiclo_kalium_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
ldiclo_labvalue_group_count
```


```{r}
ldiclo_labvalue_group_count$Labvalue_Group <-  factor(ldiclo_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
ldiclo_labvalue_dimension_of_peak_kalium <- ldiclo_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = ldiclo_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver diclo_labvalue dimension of peak-kalium", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/kalium_ldiclo_labvalue_dimension_of_peak_kalium.svg", height = 8, width = 6.5)
print(ldiclo_labvalue_dimension_of_peak_kalium)
dev.off()
```



#### Leber_Ibu ############

```{r}

libu_kalium_full_filtered <- kalium_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Ibu")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
libu_kalium_full_filtered_grouped <- libu_kalium_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 4.39 ~ "within normal reference",
      peak_labread >= 4.4 & peak_labread <= 13.19 ~ "1-2 times ULN",
      peak_labread >= 13.2 & peak_labread <= 25.39 ~ "3-5 times ULN",
      peak_labread >= 26.4 & peak_labread <= 43.99 ~ "6-9 times ULN",
      peak_labread >= 44 & peak_labread <= 219.9 ~ "10-49 times ULN",
      peak_labread >= 220 & peak_labread <= 439.9 ~ "50-99 times ULN",
      peak_labread >= 440 & peak_labread <= 2221.9 ~ "100-499 times ULN",
      peak_labread >= 2222 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
libu_labvalue_group_count <- libu_kalium_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
libu_labvalue_group_count$Labvalue_Group <-  factor(libu_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
libu_labvalue_dimension_of_peak_kalium <-libu_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = libu_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver ibu_labvalue dimension of peak-kalium", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/kalium_libu_labvalue_dimension_of_peak_kalium.svg", height = 8, width = 6.5)
print(libu_labvalue_dimension_of_peak_kalium)
dev.off()
```



#### Leber_Para ############

```{r}

lpara_kalium_full_filtered <- kalium_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Para")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
lpara_kalium_full_filtered_grouped <- lpara_kalium_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 4.39 ~ "within normal reference",
      peak_labread >= 4.4 & peak_labread <= 13.19 ~ "1-2 times ULN",
      peak_labread >= 13.2 & peak_labread <= 25.39 ~ "3-5 times ULN",
      peak_labread >= 26.4 & peak_labread <= 43.99 ~ "6-9 times ULN",
      peak_labread >= 44 & peak_labread <= 219.9 ~ "10-49 times ULN",
      peak_labread >= 220 & peak_labread <= 439.9 ~ "50-99 times ULN",
      peak_labread >= 440 & peak_labread <= 2221.9 ~ "100-499 times ULN",
      peak_labread >= 2222 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
lpara_labvalue_group_count <- lpara_kalium_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
lpara_labvalue_group_count$Labvalue_Group <-  factor(lpara_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
lpara_labvalue_dimension_of_peak_kalium <- lpara_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = lpara_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver_para labvalue dimension of peak-kalium", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  ) #-> liver_para_labvalue_dimension_of_peak_kalium


dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/kalium_lpara_labvalue_dimension_of_peak_kalium.svg", height = 8, width = 6.5)
print(lpara_labvalue_dimension_of_peak_kalium)
dev.off()


```



### kalzium #####################################################################

```{r}

kalzium_full_filtered <- kalzium_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
kalzium_full_filtered
```

```{r}
kalzium_full_filtered_grouped <- kalzium_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 2.49 ~ "within normal reference",
      peak_labread >= 2.5 & peak_labread <= 7.49 ~ "1-2 times ULN",
      peak_labread >= 7.5 & peak_labread <= 14.9 ~ "3-5 times ULN",
      peak_labread >= 15 & peak_labread <= 24.9 ~ "6-9 times ULN",
      peak_labread >= 25 & peak_labread <= 124.9 ~ "10-49 times ULN",
      peak_labread >= 125 & peak_labread <= 249.9 ~ "50-99 times ULN",
      peak_labread >= 250 & peak_labread <= 10624.9 ~ "100-499 times ULN",
      peak_labread >= 1250 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
kalzium_full_filtered_grouped
```




####labreads per Group count###
```{r}
labvalue_group_count <- kalzium_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
labvalue_group_count
```



```{r}
labvalue_group_count$Labvalue_Group <-  factor(labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
all_patients_labvalue_dimension_of_peak_kalzium <- labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "all patients_labvalue dimension of peak-kalzium", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/kalzium_all_patients_labvalue_dimension_of_peak_kalzium.svg", height = 8, width = 6.5)
print(all_patients_labvalue_dimension_of_peak_kalzium)
dev.off()
```

#### Leber_Diclo ############

```{r}

ldiclo_kalzium_full_filtered <- kalzium_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Diclo")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
ldiclo_kalzium_full_filtered
```


```{r}
ldiclo_kalzium_full_filtered_grouped <- ldiclo_kalzium_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 2.49 ~ "within normal reference",
      peak_labread >= 2.5 & peak_labread <= 7.49 ~ "1-2 times ULN",
      peak_labread >= 7.5 & peak_labread <= 14.9 ~ "3-5 times ULN",
      peak_labread >= 15 & peak_labread <= 24.9 ~ "6-9 times ULN",
      peak_labread >= 25 & peak_labread <= 124.9 ~ "10-49 times ULN",
      peak_labread >= 125 & peak_labread <= 249.9 ~ "50-99 times ULN",
      peak_labread >= 250 & peak_labread <= 10624.9 ~ "100-499 times ULN",
      peak_labread >= 1250 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
ldiclo_kalzium_full_filtered_grouped
```


```{r}
ldiclo_labvalue_group_count <- ldiclo_kalzium_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
ldiclo_labvalue_group_count
```


```{r}
ldiclo_labvalue_group_count$Labvalue_Group <-  factor(ldiclo_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
ldiclo_labvalue_dimension_of_peak_kalzium <- ldiclo_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = ldiclo_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver diclo_labvalue dimension of peak-kalzium", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/kalzium_ldiclo_labvalue_dimension_of_peak_kalzium.svg", height = 8, width = 6.5)
print(ldiclo_labvalue_dimension_of_peak_kalzium)
dev.off()
```



#### Leber_Ibu ############

```{r}

libu_kalzium_full_filtered <- kalzium_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Ibu")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
libu_kalzium_full_filtered_grouped <- libu_kalzium_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 2.49 ~ "within normal reference",
      peak_labread >= 2.5 & peak_labread <= 7.49 ~ "1-2 times ULN",
      peak_labread >= 7.5 & peak_labread <= 14.9 ~ "3-5 times ULN",
      peak_labread >= 15 & peak_labread <= 24.9 ~ "6-9 times ULN",
      peak_labread >= 25 & peak_labread <= 124.9 ~ "10-49 times ULN",
      peak_labread >= 125 & peak_labread <= 249.9 ~ "50-99 times ULN",
      peak_labread >= 250 & peak_labread <= 10624.9 ~ "100-499 times ULN",
      peak_labread >= 1250 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
libu_labvalue_group_count <- libu_kalzium_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
libu_labvalue_group_count$Labvalue_Group <-  factor(libu_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
libu_labvalue_dimension_of_peak_kalzium <-libu_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = libu_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver ibu_labvalue dimension of peak-kalzium", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  )
dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/kalzium_libu_labvalue_dimension_of_peak_kalzium.svg", height = 8, width = 6.5)
print(libu_labvalue_dimension_of_peak_kalzium)
dev.off()
```



#### Leber_Para ############

```{r}

lpara_kalzium_full_filtered <- kalzium_full %>% 
#  filter(Treatment == "Leber_Diclo") %>%
  filter(!str_detect(attribute, "_From Start|DBS"))%>%
  filter(Treatment == "Leber_Para")%>%
  mutate(`Edited_BfArM Nummer2` = as.factor(`Edited_BfArM Nummer2`)) %>% 
  group_by(`Edited_BfArM Nummer2`) %>%
  summarise(peak_labread = max(labreads, na.rm = TRUE)) %>%
  arrange(desc(peak_labread))  # optional: sortieren
```


```{r}
lpara_kalzium_full_filtered_grouped <- lpara_kalzium_full_filtered %>%
  mutate(
    Labvalue_Group = case_when(
      peak_labread <= 2.49 ~ "within normal reference",
      peak_labread >= 2.5 & peak_labread <= 7.49 ~ "1-2 times ULN",
      peak_labread >= 7.5 & peak_labread <= 14.9 ~ "3-5 times ULN",
      peak_labread >= 15 & peak_labread <= 24.9 ~ "6-9 times ULN",
      peak_labread >= 25 & peak_labread <= 124.9 ~ "10-49 times ULN",
      peak_labread >= 125 & peak_labread <= 249.9 ~ "50-99 times ULN",
      peak_labread >= 250 & peak_labread <= 10624.9 ~ "100-499 times ULN",
      peak_labread >= 1250 ~ ">500 times ULN",
      TRUE ~ NA_character_
    )
  )
```


```{r}
lpara_labvalue_group_count <- lpara_kalzium_full_filtered_grouped %>%
  count(Labvalue_Group, sort = TRUE)
```


```{r}
lpara_labvalue_group_count$Labvalue_Group <-  factor(lpara_labvalue_group_count$Labvalue_Group, 
                                   levels = c( "within normal reference","1-2 times ULN", "3-5 times ULN", 
"6-9 times ULN", "10-49 times ULN", "50-99 times ULN","100-499 times ULN",  ">500 times ULN"))

 
lpara_labvalue_dimension_of_peak_kalzium <- lpara_labvalue_group_count %>%
  mutate(n = as.numeric(n)) %>% 
  ggplot(aes(x = Labvalue_Group, y =n)) +
  geom_col() +
  theme_classic() +
  geom_text(data = lpara_labvalue_group_count, 
            aes(label = paste0("N = ", n)),
            vjust = -0.3,  # Positionierung leicht oberhalb des Balkens
            size = 5)+       # Optional: kleinere Schriftgröße
  labs(title = "liver_para labvalue dimension of peak-kalzium", 
       x = "Factor of reference value", 
       y = "number of patients") +
  theme(
    axis.text.x = element_text(angle = 45, hjust = 1)
  ) #-> liver_para_labvalue_dimension_of_peak_kalzium


dir.create("plots_labvalue dimension of rise", showWarnings = FALSE)
svg("plots_labvalue dimension of rise/kalzium_lpara_labvalue_dimension_of_peak_kalzium.svg", height = 8, width = 6.5)
print(lpara_labvalue_dimension_of_peak_kalzium)
dev.off()


```














